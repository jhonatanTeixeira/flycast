# Débitos Técnicos — flycast2021 (fork metallic77 @ 603814c9f)

> Rastreamento de status de cada achado da auditoria de performance
> (`docs/profiling_plan.md`, 2026-09-13). Atualizar o status conforme cada item for
> instrumentado/investigado/corrigido. Ver `docs/current_plan.md` para o que está
> sendo trabalhado agora e `docs/history.md` para o histórico de ações.

Status possíveis: `não investigado` · `instrumentado` · `confirmado` · `descartado`
· `corrigido`

| # | Item | Arquivo:linha | Severidade | Status | Observações |
|---|------|---------------|------------|--------|-------------|
| 1.1 | Verificação inline anti-SMC (`CheckBlock`) em todo bloco JIT executado | `core/rec-ARM64/rec_arm64.cpp:1961-2020` | ALTO→**descartado** | descartado | Teste A/B em 2026-09-13 (`block_check=false` incondicional em `driver.cpp:234`): `emuThread` e `core_average` idênticos ao baseline (26,186ms vs ~26ms) — sem efeito mensurável. Código revertido. **REAVALIADO em 2026-09-19 — o descarte estava errado:** o A/B de 2026-09-13 foi medido quando a `emu_thread` não era o caminho crítico (mesma armadilha do loop ocioso do mslug6). Com a apresentação em thread tirando a GPU do caminho no Shenmue, a emulação ficou a 99,6% de uma core, e desligar a checagem (`FC_NO_BLOCK_CHECK`, só diagnóstico) deu **20,3→24,2 fps (+19%)**, velocidade 67,7%→80,6%. Ver item 4.19. |
| 1.2 | `UpdateSystem()` chamado ~7.400×/frame pelo JIT | `core/rec-ARM64/rec_arm64.cpp:1339-1442` | MÉDIO | não investigado | |
| 1.3 | `sh4_sched_ffts()` — busca linear O(n) a cada reagendamento | `core/hw/sh4/sh4_sched.cpp:42-65` | MÉDIO→**descartado** | descartado | Instrumentado e medido em 2026-09-14: `sch_list.size()≈10-11`, custo/chamada ~0-0,5us (abaixo da resolução do timer), ~3000 chamadas/s ⇒ total ~1,3ms/s de CPU — desprezível frente aos 33ms/frame. |
| 1.4 | `bm_GetStaleBlock` — busca linear reversa em `del_blocks` | `core/hw/sh4/dyna/blockmanager.cpp:150-165` | BAIXO | não investigado | Só relevante sob SMC pesado |
| 1.5 | MMU completo (`mmu_full_lookup`) — O(64) por acesso | `core/hw/sh4/modules/mmu.cpp:338-379` | BAIXO | não investigado | Condicional a `FullMMU` ligado |
| 1.6 | Compilação de bloco / `recSh4_ClearCache` | `core/hw/sh4/dyna/driver.cpp:206-276` | BAIXO | **confirmado, impacto pequeno** | Instrumentado em 2026-09-14: soma total ~1,95s de compilação em ~159s de teste (~1,2% do tempo, concentrado em rajadas ao descobrir código novo, não distribuído uniformemente). `clearCacheCount` ficou em 2 durante todo o teste — sem thrashing de code cache. Contribuição real mas não dominante. |
| — | **NOVO: throughput real do SH4 (`SH4_SPEED_RATIO`)** | `core/hw/sh4/sh4_sched.cpp` (`sh4_sched_ffts`, medição piggyback) | — | **confirmado — achado central** | Medido em 2026-09-14: ratio fica em ~0,98-1,0 em trechos calmos (SH4 rodando em tempo real quase perfeito) mas **cai pra 0,69-0,74 em trechos pesados** — ou seja, nos momentos de mais partículas/IA, o jogo genuinamente precisa executar mais instruções SH4 por segundo real do que o Cortex-A53 consegue simular. **Não é um bug pontual — é limite de throughput bruto do host durante picos de carga do próprio jogo.** Ver seção de conclusão no topo deste arquivo. |
| 1.7 | Loop dominante do `SH4_TCB` (~40-73% do profile em kofnw/mbaa) é preenchimento de Store Queue (`_vmem_WriteMem32` por palavra, 0xE0000000-0xE3FFFFFF) | `core/rec-ARM64/rec_arm64.cpp` (`GenWriteMemory`/`GenWriteMemoryFast`, `ngen_Rewrite`, `GenMemStubs`) | ALTO→BAIXO-MÉDIO (ganho real mas modesto) | confirmado, **corrigido (fix v2, stub compartilhado via `ngen_Rewrite`)** | Identificado via `perf`+gdb ao vivo (2026-09-14): jogos enchem 2 Store Queues (8 palavras cada) com `MOV.L` individuais antes do `PREF`, e cada palavra paga uma chamada `GenCallRuntime`→`_vmem_WriteMem32` completa (salto pra fora do buffer JIT e volta) pra fazer o equivalente a um único store num buffer fixo (`sq_buffer`, já mapeado como ponteiro direto — confirmado lendo `_vmem_MemInfo_ptr[0xE0]` ao vivo). **Confirmado com `cycles` vs `instructions` sampling que não é stall (fica mais dominante em instructions, não menos) — é trabalho real, volume alto.** **Tentativa 1 (2026-09-14, revertida):** checagem inline (`addr>>26==0x38`) em `GenWriteMemoryFast` pra gravar direto em `sq_buffer`, pulando a chamada. Piorou ~6% (`core_average` 12,07→12,76-12,84ms, 2 rodadas consistentes) porque a checagem nova roda em TODO write de 32 bits, não só SQ — revertida. **Tentativa 2 (2026-09-15, aplicada, inspirada no backend x86 — ver `docs/x86jit.md`/`docs/arm64jit_improvement_plan.md` item 1):** em vez de checar em toda escrita, deixa `GenWriteMemoryFast` **intocado** e só ensina `ngen_Rewrite` — que já roda uma única vez por call-site, e só depois de um fault real — a reconhecer um fault de Store Queue (`(acc>>26)==0x38`, endereço real do fault passado do signal handler via novo campo `host_context_t::x0`/`ctx.x0`) e redirecionar aquele call-site pra um stub compartilhado (`sq_write_stub`, gerado uma vez em `GenMemStubs()`/`generate_mainloop()`, que grava direto em `sq_buffer` e cai pro `WriteMem32` genérico no caso raro de miss). `GenCallStubAddr()` (novo) espelha o cálculo de offset de `GenCallRuntime()` e usa `EnsureCodeSize(..., write_memory_rewrite_size)` pra preencher com NOP o slot fixo de 3 instruções que `ngen_Rewrite` já reserva — sem isso sobrariam bytes de lixo executados depois do `Bl`. **Build limpo (sem erro/warning novo), deploy, sanity check sem crash em kofnw e Metal Slug 6 (savestate do usuário, cena mais pesada do jogo).** **Medido com `retrorun3 --benchmark 60 --benchmark-warmup 15`, 2 rodadas por lado, mesmo savestate, só trocando o `.so`:** kofnw: `core_average` 11,40ms→11,17ms (-2,0%), `core_frames`/60s 3070→3101 (+1,0%), `core_p50` 9,95ms→9,65ms (-3,0%), `core_p95` 20,15ms→19,71ms (-2,2%), `core_p99` ~estável (ruído, sem tendência clara). Metal Slug 6: `core_average` 21,30ms→21,00ms (-1,4%), `core_frames`/60s 1917→1932 (+0,8%), `active_frame_p50` 27,41ms→25,61ms (-6,6%), `active_frame_p95` 71,08ms→69,31ms (-2,5%), `active_frame_p99` ~117,9ms→~114,4ms (-3,0%, ruidoso). **Ganho real, reprodutível (mesma direção nas 2 rodadas de cada jogo), sem regressão — mas pequeno, e concentrado no "frame típico" (p50), não na cauda pesada (p95/p99).** Deployado como binário ativo no device (`flycast_libretro.so`, backup do binário anterior em `flycast_libretro.so.pre-sq-stub-rewrite.bak`). **Conclusão importante pro pedido original do usuário (Metal Slug 6 rodando "quadro a quadro" vs PPSSPP liso na mesma cena):** esse fix NÃO explica o gap grande — a cauda pesada (p95≈70ms/p99≈115ms, ou seja frames 3-4x mais caros que o p50≈25ms) não melhorou proporcionalmente, então o que domina os picos de Metal Slug 6 é outra coisa (suspeita: renderização/draw calls em cenas com muitos sprites, não memória) — próximo alvo de investigação, não a SQ. |
| 1.8 | `lds <REG_N>,FPSCR`/`lds.l @<REG_N>+,FPSCR` sem tradução SHIL nativa (nem `rec_oph` nem `decode`), maior volume de `shop_ifb` medido (item novo abaixo) — ~196+21 hits/frame em Shenmue | `core/hw/sh4/sh4_opcode_list.cpp:240,275` (tabela), `core/hw/sh4/dyna/decoder.cpp:676-699,950-973,1024-1039` (por que cai no fallback + fim de bloco forçado) | ALTO se nativizado ingenuamente | **investigado (auditoria de código, sem implementação)** | Auditoria completa em `docs/fpscr_jit_code_audit.md` (2026-09-16, subagente read-only). Achados centrais: (1) `state.cpu.FPR64`/`FSZ64` (`decoder.h:60-61`) são capturados uma única vez por bloco (`decoder.cpp:954-955`, do FPSCR ao vivo em `driver.cpp:216`) e nunca atualizados por `lds`/`lds.l` — só `fschg` atualiza `FSZ64` mid-block, e só porque seu efeito é um XOR fixo conhecido em compile-time, não um valor de registrador; (2) `shop_sync_fpscr` (`shil_canonical.h:218-227`, codegen em `rec_arm64.cpp:520-522`, tratado corretamente por regalloc/SSA) está **completo mas nunca é emitido** (`grep "Emit(shop_sync_fpscr"` = vazio) — infra órfã, não resolve por si só o problema de `FPR64`/`FSZ64` obsoletos; (3) `UpdateFPSCR()` (`sh4_core_regs.cpp:148-154`) só troca banco FR/XF e modo de arredondamento do host, não invalida bloco nenhum; (4) não existe checagem de `fpu_cfg` no dispatch (`blockmanager.cpp:44-49`, indexado só por endereço) nem flag de invalidação dedicada a FPSCR — a segurança de hoje vem do fim de bloco forçado (`decoder.cpp:1034-1037`) + recaptura do FPSCR real em toda compilação, não de invalidação; `BET_Cond_0/1` (`decoder.h:26-27`, epílogo `rec_arm64.cpp:1289-1336`) é o precedente de forma mais próximo pra "fim de bloco condicionado a valor runtime", mas hardcoded pra `sr.T`/`jdyn`, não genérico. **Recomendação: tradução ingênua = risco ALTO** (viola a invariante que mantém FPU correta em todo o resto do JIT, sem rede de segurança em runtime pra pegar o erro). **Caminho "nativizar só quando PR/SZ não mudam, fallback idêntico ao atual quando mudam" = risco MÉDIO**, tecnicamente viável (`PR_SZ` em `sh4_if.h:271/275` já é a máscara de 2 bits pronta, nunca usada) mas exige plumbing novo (condição de fim de bloco genérica, hoje só existe pra `sr.T`). Próximo passo sugerido antes de implementar: instrumentar `fpscr.full` antigo-vs-novo nos hits reais desse opcode pra medir que fração de fato muda `PR`/`SZ` (barato, não precisa terminar a investigação de código de novo). **Validado externamente em 2026-09-16** (`docs/sh4_fpscr_external_research.md`): código real do `flyinghead/flycast` master e do `inolen/redream` (JIT SH4 independente) confirmam que nenhum dos dois compila `LDS Rn,FPSCR` sem terminar o bloco — o padrão real dos dois é idêntico ao proposto aqui (mov nativo da escrita bruta + fim de bloco forçado, bloco seguinte recompilado do zero lendo o FPSCR real), reforçando que "risco MÉDIO só com fim de bloco genérico" é o caminho certo, não um atalho arriscado. Precedente fora de SH4: Dolphin (PowerPC) também cai 100% no fallback pro interpretador pra `HID2`/paired-single-enable (o bit mais parecido com o problema do FPSCR completo), mesmo sendo um JIT 20+ anos maduro — evidência de que manter fallback aqui não é necessariamente uma lacuna a corrigir a qualquer custo. Síntese final das duas investigações (auditoria interna + pesquisa externa) em `docs/fpscr_native_translation_plan.md` (2026-09-16): recomendação foi o caminho "mov nativo da escrita + fim de bloco forçado" (reusa `dec_End`/`BET_StaticJump`, já existente), sem precisar do plumbing condicional novo que a auditoria interna cogitou — os dois JITs reais pesquisados fazem exatamente isso, incondicionalmente. **Implementado em 2026-09-16** (commit seguinte a este): duas novas `sh4dec` (`core/hw/sh4/dyna/decoder.cpp`, declaradas em `decoder_opcodes.h`) espelhando exatamente o código real do flycast master citado na pesquisa — `Emit(shop_mov32,reg_fpscr,Rn)` (ou `shop_readm` pra forma `.l`) + `Emit(shop_sync_fpscr)` + `dec_End(rpc+2,BET_StaticJump,false)` se não em delay slot — e as duas entradas da tabela (`sh4_opcode_list.cpp:240,275`) passaram a apontar `rec_oph` pra elas, igual a `frchg`/`fschg`. Build limpo, deploy, **zero crash em 2 rodadas completas de 90s+90s no Shenmue** (2470 e 2475 frames, exit 0 nas duas, ~484k execuções reais de `lds Rn,FPSCR` cobertas sem incidente). **Resultado de performance: SEM GANHO MEDIDO.** A/B mesma cena: fps 27,50→27,44 (-0,2%), `core_average` 17,759→17,853ms (+0,5%), `core_p50` 16,038→15,851ms (-1,2%), `core_p95` 32,079→32,121ms (+0,1%), `core_p99` 34,247→35,764ms (+4,4%), `active_frame_p95` 50,445→51,476ms (+2,0%), `active_frame_p99` 55,731→57,566ms (+3,3%) — sinal misto, dentro do ruído, cauda levemente pior. **Explicação provável, em retrospecto:** tanto o caminho antigo (`shop_ifb`→`GenCallRuntime(oph)`) quanto o novo (`shop_mov32` nativo + `shop_sync_fpscr`→`GenCallRuntime(UpdateFPSCR)`) pagam a mesma travessia de fronteira JIT→C++ (push/pop caller-saved + `BLR`) — só trocamos "uma chamada que decodifica `Rn` e lê memória" por "mov nativo + uma chamada um pouco mais magra", sem eliminar a travessia em si, que é o que realmente pesa. **Decisão do usuário: manter implementado mesmo sem ganho isolado** (código correto e validado como seguro, ~10 anos de precedente real em 2 JITs SH4 maduros, pode servir de base pra outro fix futuro que se beneficie da escrita já ser nativa). Binário ativo no device (`flycast_libretro.so`, backup do estado anterior em `flycast_libretro.so.pre-fpscr-native.bak`). **CORRIGIDO na v2 (2026-09-16, commit `8e8b3c80c`) — agora COM ganho real medido.** O usuário questionou o resultado nulo ("não é possível que aqui ficou pior"), e a revisão achou a premissa furada: **este fork já terminava o bloco depois de toda escrita de FPSCR desde 2015** (`decoder.cpp:1034-1037`, `OPCODE_SETFPSCR`→`dec_End`, dentro do caminho do fallback — presente no commit base `603814c9f`). Ou seja, a v1 só trocou o destino da chamada e manteve o corte de bloco: mesma travessia JIT→C++, mesmo corte. Nenhum dos dois documentos de investigação registrou isso. **Medição nova (`FC_FPSCR_STATS`, `sh4_core_regs.cpp`) deu o dado que faltava:** kofnw tem **9.628 escritas de FPSCR por frame** e **PR/SZ mudou em 0 de 11,9 milhões** (100% inalterado), com 60% das escritas não mudando nada — logo os ~9,6k cortes de bloco/frame eram 100% desperdiçados e 60% das chamadas a `UpdateFPSCR()` não faziam nada (`ChangeFP()` só roda se `FR` mudou; `setHostRoundingMode()` tem cache de `RM`/`DN`). Shenmue é o oposto: só 92 escritas/frame, mas PR/SZ muda em 88,9% delas. **A v2 adiciona dois guards no `shop_sync_fpscr` do backend ARM64:** (1) guard inline de no-op (3 instruções comparando `fpscr` com `old_fpscr`) que pula a chamada quando nada mudou — medido: no-ops chegando em `UpdateFPSCR` caíram de 7.166.131 → **2**; (2) guard de PR/SZ em runtime que só sai pro dispatcher no caminho frio, eliminando o corte de bloco incondicional (`dec_write_fpscr()` em `decoder.cpp` passa o PR/SZ esperado em `rs1` e o PC de retomada em `rs2`) — é a forma do `ir_assert_eq` do redream, já citada na pesquisa externa e descartada pela síntese v1 por causa da premissa errada. Sair do bloco no meio é seguro porque `shop_sync_fpscr` agora força writeback de **todos** os regs vivos (`ssa_regalloc.h`), mesma garantia que `shop_ifb` já exigia (e `write_back` não desaloca, então o resto do bloco segue usando registradores). Backends sem o guard (x64/x86/ARM32/cpp) mantêm o comportamento antigo via a flag nova `ngen_features::FpscrGuard` — sem bug latente. **Resultado (kofnw, savestate, 2 rodadas por lado): as 8 métricas melhoraram nas 2 rodadas** — fps +1,1%/+1,7%, `core_average` -1,9%/-3,3%, `core_p50` -0,7%/-2,3%, `core_p95` -1,2%/-1,1%, `core_p99` -2,0%/-2,7%, `active_frame_p95` -1,5%/-2,2%, `active_frame_p99` -3,0%/-1,9%. Caminho frio validado em execução real (Shenmue: guard dispara 141.828 vezes em 60s, jogo roda correto). **Shenmue não decide nada aqui:** `core_p99` do mesmo binário baseline variou 34,2→61,9ms entre duas rodadas no mesmo dia (+81%) porque o jogo boota do zero e a cena deriva — **pendência: falta um savestate do Shenmue** pra medir 3D de forma confiável. |
| — | **NOVO: contador de hits de `shop_ifb` por opcode SH4 (`FC_IFB_COUNT`)** | `core/rec-ARM64/rec_arm64.cpp:35-76,426,442-445` | — | instrumentado, **commitado** | Opt-in via env var `FC_IFB_COUNT`, dump em `/tmp/ifb-counts-<pid>.txt`. Medido em Shenmue (90s+90s warmup, 2476 frames): `lds <REG_N>,FPSCR` domina (484.809 hits totais, ~196/frame), seguido por `lds.l @<REG_N>+,FPSCR` (52.085, ~21/frame), `div1` (153.405, ~62/frame) e `tas.b` (22.983). Motivou o item 1.8. Código seguro/opt-in (sem `FC_IFB_COUNT` no ambiente, é o `OpDesc[op]->oph` direto de sempre). |
| 1.9 | `stc.l SR,@-<REG_N>` sem tradução SHIL nativa — era o MAIOR fallback pro interpretador restante nos jogos 2D depois da nativização do FPSCR | `core/hw/sh4/sh4_opcode_list.cpp:227` (tabela), `core/hw/sh4/sh4_opcode_list.h` (`DM_WriteMSRF`), `core/hw/sh4/dyna/decoder.cpp` (case novo, fallthrough pro `DM_WriteM`) | MÉDIO-ALTO | **corrigido, ganho real medido** | Achado via `FC_IFB_COUNT` em 2026-09-16: com os dois `lds ...,FPSCR` já nativos, sobrou **um único** fallback dominante — `stc.l SR,@-Rn` com **3.399.262 hits em 1431 frames no mbaa (~2.375/frame)** e ~1.935/frame no kofnw (padrão dos 2D: salvar SR na pilha em prólogo de função/interrupção). **Causa de ser o único da família sem caminho nativo:** todos os irmãos (`GBR`/`VBR`/`SSR`/`SPC`/`DBR`/`SGR`) usam `dec_STM(...)`, mas o SR neste fork é guardado **partido** em `sr.status` + `sr.T` (`sh4_sr_GetFull()`, `sh4_if.h:385`), então não dá pra ler com um mov só. Curiosamente a versão NÃO-`.l` (`stc SR,<REG_N>`) já era nativa via `dec_STSRF`/`DM_ReadSRF`, que monta o valor completo com `mov`+`or` — ou seja, metade da solução já existia. **Correção:** `DecMode` novo `DM_WriteMSRF` que emite as mesmas duas ops do `DM_ReadSRF` pro `reg_temp` (scratch já usado pelo próprio decoder no caminho nativo do `div1`) e **cai por fallthrough no `DM_WriteM`** — assim todo o store com pré-decremento, incluindo tratamento de MMU e fixup de exceção (`rn_4`), continua compartilhado com os outros `stc.l` em vez de ser duplicado. Total: ~10 linhas. **Verificado:** o opcode sumiu por completo da contagem de fallback (3.399.262 → **0**); sobraram só `div1` (~29k) e `tas.b` (~3k). **Medido (mbaa, savestate, 2 rodadas por lado, sem instrumentação): as 7 métricas melhoraram, e as duas rodadas do fix ficaram acima das duas do baseline em fps sem sobreposição** — fps 48,903→50,465 (**+3,19%**), `core_average` 13,596→13,011 (**-4,31%**), `core_p50` -4,02%, `core_p95` -5,97%, `core_p99` -6,15%, `active_frame_p50` -3,15%, `active_frame_p99` -4,54%. Sanity no kofnw sem crash e também melhor (51,66 fps). **Diferente do fix de FPSCR, este melhora a CAUDA (p95/p99 ~6%)**, que é onde os hicups aparecem. Binário ativo no device (backup do anterior em `flycast_libretro.so.pre-srfix.bak`). |
| 4.13 | ~~Invalidação de textura por página de 4KB mata ~30 texturas intactas~~ — **HIPÓTESE REFUTADA** | `core/rend/TexCache.cpp` (`VramLockedWriteOffset`) | ~~ALTO~~ → **não é problema** | **descartado (diagnóstico próprio refutado por medição)** | Registrado inicialmente como o maior gargalo do mslug6, com o raciocínio de que 13 escritas/frame viravam ~398 invalidações (29,88 por escrita) e que as texturas estariam sendo invalidadas à toa. **Isso está errado.** Erro de raciocínio: interpretei `vram_write_faults` como contagem de escritas — **fault não é escrita**. Depois do primeiro fault a página é desprotegida, então todas as escritas seguintes nela não geram fault nenhum; 13 faults podem cobrir megabytes. **Refutado por medição:** `FC_TEX_SKIP_UNCHANGED` (hash XXH32 do mesmo intervalo de VRAM que o lock vigia, pulando convert+upload se idêntico) acertou apenas **3.560 de ~174.000 updates (2%)** — em 98% dos casos o conteúdo mudou de verdade. O Metal Slug 6 faz streaming real de sprites para a VRAM todo frame; a invalidação conservadora estava **certa**. A tentativa de invalidação precisa (`FC_TEX_PRECISE_INVL`) dava +63% de fps mas **quebrava a imagem** (sprites embaralhados, verificado pelo usuário) justamente porque perdia mudanças reais — mesma natureza do speedhack do item 5.2: rápido porque pulava trabalho necessário. Código mantido opt-in e DESLIGADO apenas como registro; **não usar**. O gargalo real era outro e está no item 4.12. Detalhe completo em `docs/texcache_vram_invalidation_plan.md`. |
| 4.12 | **`settings.rend.TextureUpscale` nunca inicializado em build libretro desativava TODO o caminho de paleta na GPU** | `core/nullDC.cpp` (`LoadSettings`), `core/rend/TexCache.h:744` (`IsGpuHandledPaletted`), `Makefile:19` | **ALTO** | **corrigido — maior ganho do projeto** | Achado em 2026-09-16 instrumentando o `Update()` do cache de textura. `gpu_handled` = **0** de 166.285 texturas paletizadas no mslug6, e nenhuma desqualificada por filtro/mipmap/VQ. Causa: `HAVE_TEXUPSCALE := 0` no Makefile faz o único `settings.rend.TextureUpscale = 1` (em `libretro.cpp`) ser compilado fora; o outro candidato, em `LoadSettings()`, estava dentro de `#ifndef __LIBRETRO__` e também some numa build libretro. O campo ficava **0**. Todo o código lê `> 1` e tolera 0, menos `IsGpuHandledPaletted()` que usa `== 1` — sempre falso. **Efeito:** o caminho que deixa a GPU consumir o formato nativo (paleta aplicada no shader, como o hardware do Naomi fazia) estava permanentemente morto, forçando expansão de paleta na CPU + upload para toda textura. **Correção: uma linha** — inicializar `TextureUpscale = 1` em `LoadSettings()` fora do `#ifndef`. **Medido (savestate, 30s):** mslug6 fps 14,4→19,4 (**+35%**), `core_p95` 112,9→60,9 (-46%), `core_p99` 149,8→64,3 (**-57%**), tempo de textura 42,5→20,6ms/frame; kofnw fps 52,0→55,7 (+7%), `core_p95` -45%, `core_p99` -54%; mbaa neutro (-2%, ruído). **Também corrigiu glitches visuais** (verificado pelo usuário): o caminho antigo assava a paleta na textura no upload, então troca de paleta sem troca de dados deixava cor obsoleta; no shader a paleta é sempre a atual. Mais rápido e mais correto. |
| 1.10 | **`sq_write_stub` não salvava o LR: `Ret()` voltava para dentro do próprio stub, SP subia 128 bytes por volta até o guard page** | `core/rec-ARM64/rec_arm64.cpp` (`GenMemStubs`, caminho `not_sq`) | **ALTO — crash** | **CORRIGIDO e validado** | Crash reportado pelo usuário em 2026-09-18 no meltybld (o jogo mais liso do projeto, 60fps nas lutas) e no capsnk. **Não era regressão do trabalho de 2026-09-16** — vem do fix de Store Queue (item 1.7, 2026-09-15). **A causa:** o stub é chamado com `Bl` (LR = call site no bloco). No caminho `not_sq`, `GenCallRuntime(WriteMem32)` emite **outro `Bl`, que sobrescreve o LR**. O `Ret()` no fim então volta para dentro do próprio stub, logo depois do `Bl` interno — e cada volta reexecuta o `PopCPURegList`, cujo `ldp d16,d17,[sp],#128` **soma 128 ao SP**. O SP sobe 128 bytes por iteração até sair do topo da pilha e bater no guard page. Só dispara quando o caminho `not_sq` é tomado, que o próprio comentário do código previa como "raro a nunca" — daí ser específico de alguns jogos. **Fix: salvar/restaurar `x30` em volta da chamada** (16 bytes, mantendo o alinhamento da ABI). **Como foi achado (a cadeia importa, porque 3 hipóteses minhas caíram no caminho):** (1) `die()` descartava a razão — corrigido para logar razão/arquivo/linha, sem isso o log era só `DEBUGBREAK!`; (2) `ngen_Rewrite` assumia que todo fault no buffer JIT é acesso fastmem e, com `verify` no-op sob `-DNO_VERIFY`, seguia com `size` **não inicializada** e morria com a mensagem enganosa "1..8 bytes" — corrigido para logar o encoding e devolver `false`; (3) dump do código gerado em volta do PC + `/proc/self/maps` no handler mostraram a sequência exata e que o endereço que faltava era **guard page** entre duas pilhas de thread, com SP **acima** do topo da pilha; (4) toggle `FC_NO_SQ_STUB` confirmou por bissecção: com stub crasha, sem stub sobrevive 240s. **Hipóteses refutadas pelo dado no caminho:** "é regressão de 2026-09-16" (a baseline crasha igual), "a pilha está dentro da reserva de vmem do emulador" (está 485MB fora), "`EnsureCodeSize` estoura o slot de 3 instruções silenciosamente" (zero estouros medidos). **Validado:** meltybld 240s sem crash com o stub ativo; kofnw/mbaa/mslug6 sem crash e com performance mantida (55,6 / 50,0 / 25,5 fps). `FC_NO_SQ_STUB` fica como escotilha de emergência. |
| 4.14 | **kofxi: ~75% do trabalho do JIT é o SO cooperativo do jogo trocando de contexto em vazio enquanto espera o vblank** | `core/hw/sh4/dyna/decoder.cpp` (mecanismo `idle_hash` / detector de idle, onde uma correção entraria); código do jogo em `8C03C6A4` (switch), `8C03C6E4` (save), `8C03C7E6` (restore), `8C023040`/`8C023128` (yield), `8C022EE8` (varredura de 16 handlers) | **ALTO** | **CORRIGIDO — maior ganho de fps do projeto em 3 jogos** | **Resultado (2026-09-19, savestate, baseline × novo):** kofxi 49,7→**59,2 fps**, velocidade do jogo 82,9%→**100%**, frame p50/p95/p99 19,9/21,8/25,6→16,7/18,6/27,2ms; **MBAA** 50,0→**59,9 fps**, 83,3%→**100%**, 19,7/21,2/25,3→16,6/18,3/26,4ms; kofnw 55,6→57,0 fps, 94,4%→98,8%; mslug6 inalterado (não usa esse kernel). **A mesma função casou no MBAA e no kofnw** — é uma biblioteca compartilhada, não recurso de placa (kofxi/kofnw carregam `awbios`, MBAA não). **Os dois jogos rodavam em câmera lenta** (83% de velocidade, medido pelas amostras de áudio por segundo) — parte do que se sentia como lentidão/hicup era isso, com underrun de áudio (MBAA: 202 → 6 em 60s). **Correção:** assinatura da função `yield()` do kernel (26 opcodes, deslocamentos de `bsr` mascarados, `decoder.cpp` `idle_ff_sigs`); o bloco em +0x1E só é alcançado quando a tarefa ociosa cede para si mesma, e nele o JIT avança o tempo até **um ciclo antes** do próximo evento agendado (`sh4_sched_idle_fastforward`, pular além do evento faria o `sh4_sched_tick` nunca dispará-lo) e encerra o timeslice — mesma ideia do idle skipping do Dolphin. `FC_IDLE_FF=0` desliga, `FC_IDLE_FF_STATS` conta. **Não usa o `idle_hash` existente** por causa do item 4.17. **Efeito colateral que precisou de correção própria:** com a emulação mais rápida que a apresentação, 25% dos frames passaram a ser descartados em silêncio — item 4.16. Achado em 2026-09-18 com `FC_BLOCK_PROF` no savestate do kofxi (Atomiswave). **O que o jogo faz:** multitarefa cooperativa por corrotina. `8C03C6A4` é um "switch_to(ctx)" chamado por software que monta um frame de exceção falso (SSR=SR, SPC=PR, IMASK=F) e pula para o save completo de contexto (`8C03C6E4`: 16 GPRs, 8 bancos, GBR/SPC/SSR/MACH/MACL/PR/FPUL/FPSCR e **os dois bancos de FPU**, trocando `FPSCR.FR` para alcançar o segundo), depois restaura o contexto do próximo (`8C03C7E6`) e dá `rte`. Cada yield (`8C023128` → `8C023040`) também lê o TMU0 (`TCNT0`, `0xFFD8000C`, via `8C021DFE`) e chama uma função que varre 16 slots de handler (`8C022EE8`, `tst`/`bf`) — **os 16 estão vazios em praticamente todas as passadas** (49,13M iterações, desvio "achou" quase nunca tomado). **Números (30s + warmup, ~1.450 frames):** 3,07M trocas de contexto ≈ **~2.100 por frame**; o conjunto de blocos desse caminho = **75,3% de todas as instruções host do JIT** e ~3,13G ciclos SH4 emulados (~65% do orçamento de ciclos do período). Custo: **~1.018 ciclos SH4 e ~5.250 instruções host por troca** (5,2 host/ciclo — caro por ser quase só load/store de memória e 2 trocas de banco de FPU por lado). **Por que importa aqui e não importou no mslug6:** no kofxi a `emu_thread` **é** o caminho crítico — `rsWait` 6,3ms (main thread esperando a emulação) de um frame de 20,1ms, e a própria instrumentação do JIT derrubou o fps de 49,7 para 40,9. No mslug6 metade do JIT eliminada não mudou o fps porque o gargalo era o `Process`. **Classificação:** é espera, não trabalho — as tarefas cedem imediatamente porque aguardam o próximo frame. **Caminho de correção candidato (não implementado):** o fork já tem o mecanismo certo, `idle_hash` em `decoder.cpp` (lista de hashes de bloco de idle loop conhecidos, que recebem `guest_cycles = max_cycles*100`; já tem entradas de BIOS, SC, HH e **Dead or Alive 2**). Cobrar mais ciclos por yield faz o vblank chegar em muito menos trocas vazias. **Risco a medir antes:** o mesmo caminho de yield é usado quando uma tarefa cede depois de trabalho real; cobrar ciclos demais por yield rouba tempo emulado da lógica do jogo (sintoma: jogo em câmera lenta / música fora do ritmo). Precisa escolher o bloco e o multiplicador com medição de quantos yields acontecem fora do trecho ocioso, e validação visual/auditiva do usuário. |
| 4.15 | **Texturas paletizadas com filtro bilinear ficam fora do caminho de paleta na GPU** — era o GARGALO PRINCIPAL do samsptk; **CORRIGIDO** | `core/rend/TexCache.h` (`IsGpuHandledPaletted`), `core/rend/gles/gles.cpp` (`pp_Palette == 2` / `palettePixelBilinear`), `core/rend/gles/gldraw.cpp` | MÉDIO (kofxi) → **ALTO no samsptk** | **corrigido — samsptk 20 → 59 fps; aguarda validação visual** | Achado em 2026-09-18 com `FC_TA_SPLIT` no kofxi: 26.719 updates paletizados, só 517 no caminho GPU; 26.202 desqualificados por `dq_filter` (filtro bilinear). No mslug6 (4.12) quase tudo era *point*. **2026-09-22 (samsptk, 20 fps): virou o nº1 daquele jogo.** `perf` mostrava `convPAL4_TW` (~19%) acima do JIT; contadores: 3.070/3.670 updates/30s são `dq_filter` (84%), `dq_filter_conv_us` 8,4s/10,0s + `dq_filter_upload_us` 9,9s/11,5s ≈ 61% do tempo total. `FC_TEX_DQ_LOG`: só 3 texturas 512×512 (+2 1024×1024) reconvertidas quase todo frame. `FC_TEX_SKIP_UNCHANGED` = 0 skips (mudam de verdade). **Correção:** `pp_Palette` virou 0/1/2 (nearest/bilinear) com `palettePixelBilinear` portado do master adaptado à paleta 1024×1; `IsGpuHandledPaletted` aceita `FilterMode <= 1` quando `g_paletteBilinearSupported` (GLES3/GL3, setado em `findGLVersion`); filtro GL continua NEAREST (interpolação no shader); `FC_NO_GPU_PAL_BILINEAR=1` desliga. **Medição (mesmo binário, A/B, 2 rodadas): 20,0 → 59,4 fps, `core_average` 48,9 → 12,1ms, `core_p95` 53,7 → 12,7ms, underruns 195 → 2; `dq_filter_updates` → 0.** Neutro nos outros 8 jogos (kofnw/kofxi/ggx15/mbaa/meltybld/ggxxsla/sfz3ugd/gwing2); bateria de 10 jogos sem crash. **Pendente: validação visual** (histórico de glitch em pipeline de textura). Ver `docs/history.md` 2026-09-22. |
| 4.16 | **`QueueRender` descartava em silêncio o frame novo quando o anterior ainda estava na fila** — invisível enquanto a emulação era mais lenta que a apresentação, 25% dos frames do kofxi depois do 4.14 | `core/hw/pvr/ta_ctx.cpp` (`QueueRender`), `core/hw/pvr/spg.cpp` (`SH4FastEnough`), `core/hw/pvr/Renderer_if.cpp` (`g_rendWorkUsEma`) | **ALTO** (anulava o 4.14) | **corrigido** | Achado em 2026-09-18: com o 4.14 ligado o kofxi foi para 100% de velocidade mas **caiu** de 49,7 para 45,4 fps. Contador novo confirmou: **494 de 1.985 frames (25%) descartados** por `rqueue` ocupada — o emu thread terminava um frame antes da main thread consumir o anterior. **Referência madura:** o `flyinghead/flycast` master espera o render thread (`frame_finished.Wait()`) em vez de descartar sempre que o SH4 roda a ≥85% da velocidade nos últimos 4 vblanks (`SH4FastEnough`, `AutoSkipFrame` "normal"). Portado. **Mas só isso regrediu o mslug6** (99,7%→92,4% de velocidade na cena do boss): esperar uma main thread mais lenta que o jogo arrasta o jogo inteiro para o ritmo dela. **Tentativas refutadas no caminho (registradas porque ensinam):** (a) subir o limiar para 95% — mslug6 só foi a 95,8% e os jogos rápidos pioraram muito (kofxi 143 descartes, 55,4 fps), porque o frontend pacing por áudio faz a emulação andar em rajadas e a janela de 4 vblanks fica ruidosa; (b) medir "tempo ocupado da main thread" de pegar um frame até começar a esperar o próximo — incluía o present do frontend, que bloqueia no vsync, então marcava ~16,7ms em **todos** os jogos, igual ao intervalo de um jogo a 60Hz, e a comparação virou cara-ou-coroa (kofnw 610 descartes, 49,4 fps). **Correção final:** espera só se `SH4FastEnough` (85%, como upstream) **e** se `Process`+`Render` por frame (tempo do próprio core, nunca espera vsync) cabe no intervalo de render do jogo em tempo emulado. **Validado nos 4 jogos** (números no item 4.14; mslug6 25,5 fps @ 98,7%, igual ao baseline). `FC_AUTOSKIP=0/1/2` e `FC_FAST_ENOUGH` para A/B. |
| 4.17 | **`RuntimeBlockInfo::hash()` lê o bloco como bytes e só a primeira metade** — o mecanismo `idle_hash` existente casa blocos errados | `core/hw/sh4/dyna/driver.cpp` (`hash()`: `u8* ptr`, `ptr[i]` para `i < guest_opcodes`), `core/hw/sh4/dyna/decoder.cpp` (`idle_hash`) | MÉDIO (potencial lentidão silenciosa em algum jogo) | **confirmado — não corrigido** | Achado em 2026-09-19 ao avaliar o `idle_hash` para o 4.14. O hash deveria cobrir os opcodes (u16) do bloco; cobre `guest_opcodes` **bytes**, cada um estendido para u16, e o teste de `mov.l @(disp,PC)` (`>>12 == 0xD`) nunca é verdadeiro para um byte. Num bloco de 4 opcodes o hash cobre só os 2 primeiros — p.ex. `mov r15,r4 / add #4,r4`, abertura de qualquer função C que pega endereço de local. **Consequência possível:** as entradas existentes do `idle_hash` (BIOS, SC, HH, "suspeitas", Dead or Alive 2) podem estar casando blocos que não são idle loops em outros jogos, e cada casamento cobra `max_cycles` (224) ciclos por execução — o jogo perde tempo de CPU emulada sem nenhum sintoma além de lentidão. O `flyinghead/flycast` master removeu esse `hash()`. **Não medido ainda:** quantos casamentos acontecem por jogo (basta logar no `strstr` do decoder). |
| 4.18 | **Shenmue (cena de 20fps do usuário): CPU e GPU não se sobrepõem — o present do frontend espera a GPU terminar o frame inteiro, e o frame vira CPU + GPU em série** | frontend `retrorun3` (caminho de vídeo `fallback`, `framebuffer=frontend FBO`); no core: `core/rend/gles/gldraw.cpp` (586 draw calls/frame), `core/rend/gles/gles.cpp` (`FC_GL_FINISH`) | **ALTO** | **CORRIGIDO no frontend (fork do retrorun) — instalado no device para avaliação jogando** | **Correção (2026-09-19, fork `jhonatanTeixeira/retrorun`, branch `threaded-present`):** apresentação em thread no backend SDL — a thread principal compõe o frame (core, shader, overlays, menus) numa textura offscreen, cria uma fence e entrega para uma thread de apresentação com contexto GL compartilhado, dona da janela, que é quem fica bloqueada no `SDL_GL_SwapWindow`; o contexto do core fica corrente sem surface (`EGL_KHR_surfaceless_context`, confirmado no blob Mali r13p0). Compilado localmente (cross aarch64 + sysroot extraído do device, sem build no device). **Medido (mesmo binário, síncrono × thread):** Shenmue 17,1→**21,1 fps**, velocidade do jogo 57%→**71%**, intervalo real na tela p50/p90/p95/p99 59,4/60,3/61,0/64,2 → **46,9/49,1/50,5/54,1ms** (com vsync + fila FIFO). **O que a medição ensinou no caminho:** (a) sem vsync, tirar o swap da thread piora jogos que já rodam a 100% (kofxi: frames >25ms 30→62-103, áudio atrasado >10ms 37→323-465) — o swap síncrono era o que dava ritmo ao loop, e sem ele o loop fica regulado só pelo backpressure do áudio, em rajadas; (b) fila de uma vaga × mailbox não fez diferença (só 14 frames substituídos em 30s) — a causa era pacing, não bloqueio da fila; (c) com vsync a tela vira o relógio e a política certa é FIFO (a vaga abre a cada vblank); sem vsync, mailbox. O kofxi síncrono também já estourava o orçamento: core 8,9ms + swap 8,5ms = 17,4ms > 16,7ms — é a origem dos hicups que o usuário já via antes. Métrica nova no fork: `RETRORUN_PRESENT_STATS=1` (intervalo entre swaps; sob vsync ela oscila por causa da espera do flip anterior dentro do KMSDRM, e discordou da percepção do usuário no kofxi — o usuário viu melhora, underruns de áudio 12→3). **Instalado:** `/usr/local/bin/retrorun3` = build do fork (backup `retrorun3.bak-3.1.2`), `dreamcast.sh`/`naomi.sh` exportam `RETRORUN_VSYNC=1 RETRORUN_SDL_THREADED_PRESENT=1` (backups `*.bak-threaded`); `retrorun.cfg` global intocada (N64/Saturn usam o mesmo `retrorun3`, sem vsync nem thread). **O que sobra no Shenmue:** a CPU emulada — com a GPU fora do caminho a main thread espera a emulação 22,5ms/frame; os blocos quentes são o laço de transformação de vértices do jogo (`ftrv`/`fipr`/`fdiv`), trabalho real, frente de JIT de FPU. Medido em 2026-09-19 no savestate do usuário (cfg real): **17,2 fps, jogo a 57,7% de velocidade**, frame p50/p95/p99 57,9/59,7/62,4ms. **A CPU emulada sobra** (main thread espera a emulação só 2,3ms/frame) — não é o caso do kofxi. Mapa: `Process` 4,6ms, `Render` (envio GL) 19,6ms, **vídeo do frontend 34,0ms** (nos jogos 2D é ~7,5ms). Instrumento novo `FC_GL_FINISH` (`glFinish` cronometrado no fim do `Render`): **GPU = 35,1ms/frame** e o vídeo do frontend cai para 2,6ms — os 34ms eram a GPU. A 320x240: GPU 14,7ms ⇒ ~27ms de fragment/fill-rate + ~8ms fixos (geometria/tiler) a 640x480. Envio GL **~20ms independente da resolução**: 586 draw calls × ~35µs (custo por chamada do driver Mali JM, cf. item 4.2). **O ponto central:** 58ms ≈ (4,6 + 19,6) CPU **+** 34 GPU — em série. Com sobreposição (CPU monta N+1 enquanto a GPU desenha N) seria ~max(25, 35) ≈ 35ms, perto dos 30fps do jogo. **O core não sincroniza** (grep: nenhum `glFinish`/`glFlush`/fence por frame; `glReadPixels` só no RTT, desligado) ⇒ a espera está no caminho de apresentação do frontend. **Alavancas, por tamanho:** (1) frontend deixar a GPU correr um frame à frente (código do retrorun3 não está neste repo); (2) fill-rate no core — item 1 do `rendering_improvement_plan.md` (`glInvalidateFramebuffer`, nunca implementado) e custo de fragment; (3) draw calls. |
| 4.19 | **Checagem anti-SMC inline em blocos de páginas "sujas" custa ~19% no Shenmue — e as páginas são sujas por DADO, não por código automodificável** | `core/hw/sh4/dyna/blockmanager.cpp` (`SetProtectedFlags`, `bm_RamWriteAccess`, `bm_ReprotectQuietPages`), `core/rec-ARM64/rec_arm64.cpp` (`CheckBlock`, `ngen_Rewrite`), `core/hw/sh4/dyna/driver.cpp` | **ALTO** | **CORRIGIDO — ligado por padrão após bateria de 11 jogos sem crash** (`FC_CODEPAGE_STORES=0` desliga) | **Implementação (2026-09-19):** (1) stores do JIT (formato fastmem) que faltam numa página de código travada, escrevendo fora do código, são reescritos (`ngen_RewriteCodePageStore`) para stubs por tamanho com caminho rápido inline — página não travada ou trecho de 32 bytes sem código (bitmap `bm_code_chunks`) escreve direto num **espelho da RAM que nunca é travado** (`0x0C000000 + RAM_SIZE`); só trecho com código vai para C (checagem exata: SMC real segue o caminho antigo); (2) stores em **endereço constante** (`GenWriteMemoryImmediate`, forma `ldr x0,=ptr; str w,[x0]` que o rewrite não alcança) usam o espelho já na compilação, registrados por trecho — um bloco compilado depois sobre o trecho descarta esses escritores; (3) **leituras dobradas pelo SSA** (`ssa.h`: bloco `read_only` lê endereço fixo da própria página e embute o valor) são registradas (`bm_CanFoldRead`); escrita sem fault num trecho dobrado descarta quem dobrou e marca o trecho como volátil (nunca mais dobrado); trecho com store constante aliasado nunca é dobrado. **Erros no caminho (registrados porque custaram):** (a) primeira versão mandava toda escrita de dado em página travada para C: 8,7M chamadas em 40s, Shenmue 20,3→11,2 fps → bitmap inline; (b) stores em endereço constante não eram reescritos e continuavam desprotegendo a página → alias na compilação; (c) **o alias quebrou kofxi e MBAA no boot** (`iNimp` na BIOS, `SH4ThrownException`, abort): eu não tinha considerado que o SSA embute leituras de páginas protegidas — o kernel de tarefas lê variáveis coladas no próprio código, e a escrita pelo espelho não invalidava o valor embutido → registro das leituras dobradas; (d) essa correção, ingênua, fazia o store constante voltar ao caminho travado quando o trecho estava dobrado, desprotegendo a página de novo (Shenmue 19,9 fps) → trecho escrito por store constante não é mais dobrado. Durante (c)/(d) o core do device ficou com a mudança **desligada por padrão**. **Medido com a mudança ligada:** Shenmue 20,3→**23,3 fps**, velocidade 67,7%→**77,7%**, cadência p50/p95/p99 48,8/54,0/59,0→42,4/47,3/51,9ms, **0** blocos com checagem (eram 490); kofxi 59,7 / MBAA 60,0 fps sem crash. Achado em 2026-09-19. `perf` da `emu_thread` no Shenmue: **80% é código gerado pelo JIT** (AICA ~8%, ARM7 ~2%, TA ~1%). Ferramenta nova `FC_DUMP_BLOCK=<vaddr>` grava o ARM64 gerado de um bloco: no bloco mais quente do laço de vértices, **22 de ~142 instruções** são a comparação inline dos 36 bytes do código SH4 a cada execução. Ela existe porque a página (4KB) foi escrita depois de conter código: o fork a marca desprotegida para sempre e todo bloco compilado nela carrega a checagem. No Shenmue: só **490 de ~13.500 blocos**, em **9 páginas**, mas justamente os mais quentes. **Tentativa 1 (implementada, mantida, sem ganho no Shenmue):** reproteger páginas quietas estilo PCSX2 (`FC_SMC_REPROTECT`, backoff exponencial, desiste após 6) — 18 reproteções, **17 re-faults**. **Por quê (log dos re-faults):** 100% das escritas caem **fora** do código compilado — são dados na mesma página do código (ex.: `0x0C1EDA80`, variável logo antes da rotina em `0x0C1EDAEA`, escrita por `0x0C1ED4E0`). Não há código automodificável; a checagem é custo puro e a granularidade de página não separa dado de código. **Correção proposta:** quando um *store do JIT* faltar numa página de código protegida, reescrever só aquele store (via `ngen_Rewrite`, como o stub de SQ do item 1.7) para um caminho lento que confere se o endereço cai dentro de código compilado (invalida se sim, só escreve se não) — a página fica protegida, os blocos sem checagem inline; escritas de fora do JIT (DMA, C) seguem o caminho atual. Prêmio medido: +19% fps / +13 pontos de velocidade no Shenmue. |
| 4.20 | **DOA2 (Dreamcast, 60fps): preso pelo THROUGHPUT DE EMULACAO SH4 (emu thread saturada em 1 core), com o render (640 draw calls) como 2o gargalo que descarta frames** | `core/hw/sh4/dyna/*` (JIT SH4), `core/rec-ARM64/rec_arm64.cpp` (regalloc FPU), `core/rend/gles/gldraw.cpp`, frontend retrorun (`platform_sdl.cpp`) | **ALTO** | **diagnosticado — nao corrigido** | 2026-09-22. Baseline no savestate: **23,1 fps, jogo a 73,9% de velocidade**, 186 underruns. **Correcao de leitura:** a 1a conclusao ("render thread e o limitante") estava ERRADA. A medicao que decide e a **amostragem de CPU por thread** (`/proc/<pid>/task/*/stat`): **emu thread ~103% de um core (saturada)** vs main/render ~72%; confirmado por `perf --sort pid` (emu roda `SH4_TCB` 39% self + `ta_vtx_data32` + `ARM7_TCB`; main roda `FifoSplitter`/`make_index`/`DrawStrips`/`libmali`). CPU a 1,512GHz (max), sem throttling. **`req_native_fps=59,92`, `req_rtt=0`, `req_display=1126`** (contador novo, imune a drops): o jogo pede 60 frames de tela/s em tempo emulado, sem RTT — DOA2 e 60fps nativo (a `swap_native_fps=31,65` inicial estava contaminada pelos 521/591 drops). **Quadro final:** a emu thread produz ~40 frames reais/s onde o jogo pede 60 = **~74% de velocidade**; o render (640 draws, 22,5ms, CPU-bound nao-GPU: `FC_GL_FINISH` ~8ms, indiferente a resolucao) descarta os que nao cabem -> tela ~21fps. **Remover o render nao levaria a 60fps** — o teto de 74% e do SH4 (mesmo caso do Shenmue em cena pesada). 4 combinacoes de vsync/threaded present: todas ~74%. `FC_DUMP_BLOCK=8C101BC4` (bloco #1, 10,4% do trabalho): laco de transformacao de vertices; `ftrv` ja em NEON, mas `fldi0/fldi1/fadd/fmov.s` fazem muito trafego `[x28,#...]` (regs FPU nao alocados, item 4.9). JIT espalhado (~393M host-ops/s). **Pista aberta:** blocos com `cycles=224` sao matches do `idle_hash` (entrada "Dead or Alive 2"), e o item 4.17 suspeita que esse hash case blocos errados. Ver `docs/history.md` 2026-09-22. |
| 4.21 | **KOF Evolution (DC, chuva): jogo a 100% mas tela a 30fps — render (~21ms) passa dos 16,7ms e a fila de UM slot joga fora o frame que chega durante o render** | `core/hw/pvr/ta_ctx.cpp` (`QueueRender`/`FinishRender`, `rqueue`), `core/rend/gles/gldraw.cpp` (`DrawList`) | ALTO | **confirmado (medido), nada corrigido** | Medido 2026-09-22 (savestate, `--benchmark 30`, `FC_REND_SPLIT`): 30,4 fps, áudio 100%, core p50/p95/p99 31,6/34,6/41,7ms. `req_native_fps` 59,8 (o jogo é 60), `emu_frame_interval` 17,0ms (emu acompanha), só 1045 de 2068 frames renderizados. Main thread: `rsWait` 10,3 + `process` 2,1 + `render` 19,0ms, 488 draw calls (quebras de lote: `tcw` 450/frame). CPU por thread: emu 102%, main 71% — mesma assinatura do DOA2, mas aqui a emulação dá conta. **Mecanismo:** o `ctx` só sai do `rqueue` no `FinishRender`, depois do `Render()`; o frame N+1 chega aos 17ms, o slot está ocupado, é descartado, e o render espera ~10ms pelo N+2 → 60 vira 30. Duas alavancas: (a) fila de 2 slots (render pega o mais recente assim que termina, ~47fps em cadência irregular — precisa avaliação jogando); (b) render < ~13,5ms dá 60 de verdade. **Driver:** 71% da CPU da main thread está dentro da `libmali`; ~10% é UM laço em `0x62fba0` que varre o index buffer (u32, pula `0xFFFFFFFF`) calculando min/max a cada draw. **Implementado, NÃO medido:** `glDrawRangeElements` (via `eglGetProcAddress`) em `DrawList`, passando o min/max que já calculamos; `FC_NO_DRAW_RANGE=1` volta ao `glDrawElements`. Binário de teste em `/home/ark/flycast_test.so` (core ativo intocado). |
| 4.22 | **mslug6 (savestate do trem): BIMODAL — mesma cena, rodadas caem em ~20 fps a 66% ou ~20 fps a ~99% de velocidade** (confirmado 2026-09-23: old1 20,6 fps/99,3%/16 underruns × old2 19,9 fps/66,6%/222 underruns, mesmo binário — exatamente o "16 fps a 100% e fluido" que o usuário viu) | `core/hw/pvr/Renderer_if.cpp` (`rend_frame`: `re.Set()` só depois do `Process`), `ta_ctx.cpp` | MÉDIO | **corrige a nota de 2026-09-19** | 4 rodadas iguais (2026-09-22, `--benchmark 30`, mesmo binário): 19,8-19,9 fps, 66,2-66,5% de velocidade, core p50/p95/p99 ~48,9/53/59ms. O usuário confirma: sempre ~20fps na cena, volta a 30 depois que o trem passa; o que muda é fluidez/sensação de velocidade. **Mecanismo candidato (não medido):** a emu thread fica bloqueada durante o `Process()` (~22ms, textura) de cada frame renderizado (`re.Set()` depois do Process) e o ciclo vira Process 22 + max(emu, render 19,7) ≈ 49,4ms; a fase entre emu/render/vsync pode mudar a cadência por rodada. Medir com `RETRORUN_PRESENT_STATS` + velocidade por segundo com timestamp, separando o trecho do trem. **Erro de método registrado:** amostrar o `/tmp/rend-split-*.txt` (reescrito a cada 150 frames) a cada 5s deu "30fps/100%" falso — usar o mtime do arquivo ou contadores com timestamp. |
| 4.23 | **MBAA: glitches — blocos no retrato e lixo tipo texto (fixos) + retrato sumindo/só contorno (intermitente)** | `core/rend/TexCache.cpp`, `core/rend/gles/gldraw.cpp` | ALTO (visual) | **investigando** | 2026-09-22, savestate slot 2 do usuário (`mbaa.fc2021-rrstate.auto1`), dump com `FC_FB_DUMP`. **Fixos (aparecem no dump, iguais em todas as rodadas):** blocos retangulares claros no retrato da Akiha e "texto" vermelho de lixo no fundo à esquerda. **Descartados como causa desses:** RTT (`req_rtt`=0; e `reicast_enable_rtt` nem existe neste core, só `_rttb`); cache de textura (`FC_TEX_ALWAYS_UPDATE=1`, novo, reenvia toda textura 1x/frame: frame 0 idêntico, blocos continuam); sorting (`per-triangle`: 0 pixels diferentes). **Intermitente:** o retrato some e fica só o contorno em algumas rodadas e não em outras, no mesmo save — depende de tempo → suspeita de corrida emu×render ou de frame descartado. Com o dump a cada frame (lento, ~5fps) não reproduziu em 2 rodadas. Próximo teste: várias rodadas com `reicast_threaded_rendering = disabled` (e/ou `FC_AUTOSKIP=0`) observadas pelo usuário. **Pista forte (4.31): o flycast oficial não tem esses glitches.** |
| 4.24 | **"Teto de SH4" comum a FMV e DOA2 é IPC ~0,5 no A53 — mas a parada é DIFERENTE em cada um** | `core/rec-ARM64/rec_arm64.cpp` (densidade do código gerado), `core/hw/sh4/dyna/ssa_regalloc.h` | ALTO | **confirmado (medido)** | 2026-09-23, contadores do Cortex-A53 (`perf stat -t <emu_tid>`, raw r01/r03/r05/r08/r10/r17), 3 rodadas do RE CV do boot (idênticas) + DOA2 no savestate. **Clock real ~1,30GHz** com a thread saturada, mesmo com `scaling_cur_freq`=1512000; governor `performance` × `ondemand` = resultado idêntico na FMV (21 frames/s em vez de 30) → governor descartado. **FMV (RE CV):** IPC 0,50; **L1I miss 16-25/1000 instr** (vs ~3 fora da FMV); 79% dos misses no código do JIT; o código quente traduzido ocupa **60KB (80% das instr) / 115KB (90%)** contra 32KB de L1I 2-vias — o decoder Sofdec cabia no I-cache de 16KB do SH4, e a tradução (~3,2 instr ARM de 4B por instr SH4 de 2B, ~6x) transborda. Misses espalhados ~1% por bloco (`8C20Axxx`/`8C20Bxxx`): não há bloco vilão, o conjunto não cabe. **DOA2:** IPC 0,50; L1I 4,7/kI; **L2→DRAM 4,9/kI** (~0,7 ciclo/instr só nisso), 76% vindo do código do JIT (acessos do jogo à RAM emulada; o r17 também conta refill de instrução). **Conclusão:** a análise "3,2 host ops por op SH4" não fecha sozinha (daria folga ~2x); o que pesa são as paradas. Alavanca comum: **densidade do código** (menos preload/writeback de contexto = menos bytes = cabe mais no L1I e menos acessos). Medir por L1I/kI e bytes host por instr SH4, não só por contagem. **Testado e descartado como alavanca:** repassar `PREF` fora da SQ como `PRFM` (antes descartado pelo JIT): A/B DOA2 73,1/75,4% × 75,9/75,8% — ruído. Mantido (`FC_NO_PREF_HINT=1` desliga), só no binário de teste. |
| 4.25 | **Descompressão do CHD (LZMA + recálculo de ECC) roda síncrona na thread de emulação** | `core/imgread/chd.cpp`, `core/deps/libchdr` | MÉDIO | **confirmado, não corrigido** | FMV do RE CV (2026-09-23, amostragem por instrução na emu thread): `LzmaDec_DecodeReal2` 3,7% + `ecc_compute_bytes` 3,1% + `inflate_fast`/`crc16` ≈ **~8% das instruções** da thread já saturada, com 3 cores ociosos. O ECC recalculado é inútil para leitura de dados de 2048 bytes. Correções candidatas: decodificar hunks numa thread à parte (prefetch sequencial) e/ou pular a regeneração de ECC quando o setor pedido é só dados. |
| 4.26 | **FMV do RE CV: o SH4 emulado está ~88% ocupado com código real do jogo (171M instr SH4 por segundo emulado) — carga pesada para o próprio Dreamcast** | `core/hw/sh4/dyna/blockmanager.cpp` (`FC_BLOCK_PROF`, agora grava `.all` com todos os blocos e o código SH4 dos top-16 por tempo EMULADO) | ALTO (explica o teto da FMV) | **confirmado (medido)** | 2026-09-23, boot do RE CV, diff de snapshots de `FC_BLOCK_PROF` só na janela da FMV (24,5s wall = 14,8s emulados, 61% com o profiler). **171M instruções SH4 por segundo emulado**, 1,17 ciclo cobrado por instrução → ~200M ciclos/s: o SH4 real também estaria praticamente cheio. Onde vai o tempo EMULADO: ~50% blocos do decoder (`8C20Axxx`-`8C20Bxxx`, IDCT/VLC), 9,8% idle-loop já barato (`8C1787C2`, lê timer, cobrado 224 ciclos por volta, 0,4% do host), ~2% varredura de tabela da middleware CRI (`8C17282A`↔`8C17287E`, testa flag por entrada — padrão do kofxi, ~6% do host), divisão por software (`8C132A1A`). **Não é espera disfarçada.** Custo nosso: 4,9 instr host por instr SH4 (com o contador do profiler), IPC 0,5, 1,3GHz → ~9 ciclos host por instr SH4 → teto ~145M instr SH4/s ≈ 85%; o resto até os ~70% medidos é C++ na emu thread (UpdateSystem, SR, AICA/ARM7, CHD — item 4.25). **Correção de leitura:** blocos como `8C20A496` (28 instr SH4, 155 host, 3 ciclos cobrados) pareciam 50x mais caros "por ciclo", mas é só o modelo de ciclos (várias ops cobradas 0); por instrução SH4 são 5,5x, normal. Alavancas: menos ciclos host por instr SH4 (densidade do código, item 4.24) e tirar trabalho não-SH4 da emu thread (4.25). |
| 4.27 | **Acesso à RAM emulada custava 5 instruções ARM (mov endereço→w0, add offset, ldr/str, nop de reserva, mov resultado) onde cabe 1** | `core/rec-ARM64/rec_arm64.cpp` (`GenReadMemoryCompact`/`GenWriteMemoryCompact`, `GenCompactTrampoline`, `DecodeCompactMem`/`RewriteCompactMem`, `EnsureMemBase`) | **ALTO** | **implementado, em validação (binário de teste)** | Achado 2026-09-23 dumpando os blocos quentes da FMV do RE CV (`FC_DUMP_BLOCK`): no `8C20AB70` (58 instr SH4), 32 acessos × ~5 instr = ~128 de 235 instruções ARM. O formato existia porque o rewrite em fault precisava do endereço em `w0` e de um slot fixo. **Correção:** base da RAM emulada (`x28 + sizeof(Sh4Context)`) em `x13` (não usado pelo JIT), emitida uma vez e revalidada só depois de `Bl`/`Blr`/`Bind` (wrappers na classe); acesso = `ldr/str wD, [x13, wA, uxtw]` com os registradores alocados direto (floats via `w15`; 64 bits via `x15`+contexto). No fault, `ngen_Rewrite`/`ngen_RewriteCodePageStore` reconhecem a forma (Rn=x13, option UXTW), geram um trampolim no fim do cache de código (salva LR + v16-v31, `w0`=endereço, `w1`=dado, chama ReadMem/WriteMem ou o stub de SQ/página de código, restaura `x13`) e trocam SÓ a instrução por um `b`. `FC_NO_COMPACT_MEM=1` volta ao antigo; `FC_COMPACT_LOG=1` loga os rewrites. **Código:** `8C20AB70` 940→464 bytes (-51%), `8C20AF58` 1300→696 (-46%), `8C20A5B2` (FPU) 804→672 (-16%). **Medido (FMV do RE CV, boot, 2 rodadas por lado intercaladas, mesmo binário):** 21,4/21,3 → **24,5/25,0 frames/s = 71% → 82-83% de velocidade**; confirmado com o binário final (21 → 25 frames/s). **Evolução no caminho:** (1) 1ª versão do trampolim salvava LR + v16-v31 em TODA execução de site reescrito → bateria mostrou Shenmue 76→68%, Soulcalibur 91→79%, KOF Evo 99,5→95%; corrigido salvando só os S16-S31 vivos no site (máscara gravada na emissão, `compact_live_fregs`, limpa em `ngen_ResetBlocks`) e nada para os stubs; (2) acesso de 64 bits (`fmov.d`) também compacto; (3) `ZeroExtendLoadPass` (ssa.h, `FC_NO_ZX_LOAD=1`): `mov.b`/`mov.w` + `extu` → `ldrb`/`ldrh`; `flags2` do `shil_opcode` agora inicializado; (4) trampolim especializado de Store Queue (escrita direta no `sq_buffer`). **Bateria final, 18 jogos com savestate, 20s, novo × `FC_NO_COMPACT_MEM=1`: zero crash, todos os states carregados.** DOA2 74,1→75,4%, SA2 96,7→97,7%, Soulcalibur 88,3→92,6%, meltybld 79,9→87,2%, KOF Evo 30,4→31,1 fps, sfz3ugd p99 17,8→10,9ms; Shenmue/Skies/Grandia/Le Mans/gwing2/mbaa/samsptk/ngbc empate (os p95 piores de samsptk/ngbc na bateria NÃO se repetiram em 2-3 rodadas por lado — cauda desses jogos varia por rodada). mslug6: ~22,5 fps nos 2 lados novos × 19,9-20,6 antigos (+10%); a velocidade é bimodal (ver 4.22). **kofnw:** fps 54-57 novo × 56-59,5 antigo em 6/6 pares, mas a emu thread executa **8,7% MENOS instruções e 9% menos ciclos** (perf stat, 2×2) e com instrumentação deu 56,0 × 56,3 — não é custo do JIT, é interação com a fila de render (item 4.29). |
| 4.28 | **BUG latente: o slow path reescrito após fault não salvava os floats caller-saved (S16-S31)** | `core/rec-ARM64/rec_arm64.cpp` (`ngen_Rewrite` → `GenReadMemorySlow`/`GenWriteMemorySlow` num `Arm64Assembler` novo), `Arm64RegAlloc::PushCallerSaved` | MÉDIO (correção) | **contornado pelo 4.27 nos acessos compactos; caminho antigo inalterado** | Desde o item 4.9 (S16-S31 alocáveis), `GenCallRuntime` salva só os S16-S31 que o regalloc DO BLOCO usa. No rewrite, o assembler é criado do zero (`reg_alloced` vazio) → `PushCallerSaved()` não salva nada → o C++ chamado (ReadMem/WriteMem) pode destruir floats vivos do bloco. Os stubs de SQ e de página de código salvam tudo explicitamente; o slow path genérico não. Os trampolins do 4.27 salvam v16-v31 sempre. Candidato a explicar glitches intermitentes em 3D (não verificado). |
| 4.29 | **kofnw: emulação mais barata (4.27) não vira mais fps — a fila de render descarta/espera diferente** | `core/hw/pvr/ta_ctx.cpp` (`QueueRender`, autoskip), `core/hw/pvr/Renderer_if.cpp` | MÉDIO | **confirmado; paliativo instalado SEM medição (margem de 75%)** | **Paliativo 2026-09-23 (pedido do usuário, sem A/B por falta de tempo):** a emu thread só espera o render se ele usar ≤ 75% do intervalo do jogo (`FC_AUTOSKIP_MARGIN`, 100 = regra antiga). Objetivo: KOF Evolution na chuva voltar a 100% de velocidade mantendo o glDrawRangeElements (antes: 54 fps a 92%). kofxi/MBAA (~6ms de 16,7) devem seguir esperando — **não verificado**. Instalado como `flycast2026_libretro.so` (anterior em `.bak-pre-margin`). Medir: KOF Evo chuva, kofxi, MBAA, kofnw, com `FC_AUTOSKIP_MARGIN=100` × padrão; cfg de benchmark novo `~/retrorun_dbg3.cfg` (chaves `flycast2026_`) e `~/bench_2026.sh` já criados no device. | 2026-09-23: com o acesso compacto a emu thread do kofnw gasta 7,95s de CPU em 15s contra 8,6-8,7s (-9% de instruções e ciclos), velocidade 100% nos dois, mas fps 54-57 × 56-59,5 em 6 pares sem instrumentação; com `FC_REND_SPLIT`+`FC_IDLE_FF_STATS` deu 56,0 × 56,3 (drops 151 × 143, `waited_for_render` 478 × 343). A cauda do kofnw já é bimodal no baseline (p95 12 ou 33ms conforme a rodada). Mesma família do 4.16/4.22: a fase entre emu, render e vsync decide quantos frames chegam à tela. |
| 4.30 | **Soulcalibur congela no BOOT com flycast2026 lançado pelo ES** | a investigar (candidatos: acesso compacto 4.27 / `ZeroExtendLoadPass` / `glDrawRangeElements` — todos com chave de desligar) | ALTO | **não investigado (usuário pediu para não corrigir agora)** | 2026-09-23: `dreamcast.sh retrorun3 flycast2026` com o `retrorun.cfg` oficial (sem `auto_load` → boot do zero): velocidade 40%→99%→72% e congela **na tela antes da luta** (carregamento da luta da demo, visto pelo usuário na tela); processo ficou vivo (log em `/home/ark/soulcal_freeze_flycast2026.log`). Na bateria de regressão (savestate) o Soulcalibur rodou limpo — o save já começa no meio da luta e pula esse carregamento (GD-ROM + DMA), então a bateria de savestates não cobre telas de loading: adicionar um boot/transição à bateria. Primeiro passo: repetir o boot com `FC_NO_COMPACT_MEM=1`, depois `FC_NO_ZX_LOAD=1`, depois `FC_NO_DRAW_RANGE=1`, e com o `.so` anterior (`flycast_libretro.so.bak-fork-pre-upstream`) para saber se é regressão. |
| 4.31 | **RESSALVA p/ o futuro: o flyinghead/flycast oficial (v2.7-42) roda o MBAA SEM os glitches gráficos (a ~34 fps)** | comparar `core/rend/gles/*`, `core/rend/TexCache.*`, `core/hw/pvr/*` (fork) × upstream | ALTO (pista para o 4.23) | **não investigado** | Observado pelo usuário em 2026-09-23 jogando no device, com o upstream instalado como `flycast_libretro.so`. Duas frentes: (1) **glitches do MBAA (4.23):** como o upstream renderiza certo a mesma cena, a diferença de código de render/textura/TA entre os dois é a pista mais direta que já tivemos — dumpar o mesmo frame (savestate não é compatível entre os dois; usar o mesmo ponto do jogo) e comparar o que cada um faz com as texturas/polígonos dos blocos do retrato; (2) **lentidão do upstream:** hipótese do usuário de que só o patch da lib de concorrência (threads/sincronização) talvez já corrija o desempenho do flyinghead no device — a investigação de 2026-09-13 viu frames escalando de 26 para 100ms antes do SIGSEGV no dispatch do JIT, compatível com problema de concorrência. Se confirmado, o upstream vira alternativa real (correto nos gráficos) e/ou referência para portar correções. |
| 4.32 | **Rewrite usava `x0` como endereço guest — errado no acesso compacto → escritas de Store Queue caíam no C++ genérico** | `core/libretro/common.cpp` (handler ARM64), `core/rec-ARM64/rec_arm64.cpp` (`ngen_Rewrite`) | ALTO | **corrigido** | 2026-09-24. O handler passava `ctx.x0` como `acc`; no caminho antigo o endereço estava sempre em `w0`, no compacto (4.27) está em `wRm`. O teste de SQ (`acc>>26==0x38`) olhava lixo → quase todo site de SQ virava trampolim de `WriteMem32` em C++ (explica a perda do kofnw no 4.29). Agora `acc = si_addr - virt_ram_base` (espaço de 4GB), válido para os dois formatos. Junto: trampolim de SQ aceita escrita de **64 bits** (`fmov.d`, 116 sites no DOA2) e **trampolim da RAM interna do SH4** (OCRAM 0x7C-0x7F, `OnChipRAM[addr&0x1FFF]` com checagem de `CCR.ORA`; ~250 sites no DOA2; `FC_NO_OCR_FAST=1` desliga). **Medido (1 rodada por jogo, savestate):** DOA2 **72,9% → 80,9%** de velocidade, Shenmue (cena pesada) 76,0% → 79,7-79,9%, KOF Evo 100%, Zombie Revenge sem mudança (74%). 2D: SFA3 98,6%, MBAA 97,9%, KOFXI 99,8% — iguais. |
| 4.33 | **Fila: liberação antecipada do slot + só enfileirar com render quase no fim** | `core/hw/pvr/Renderer_if.cpp` (`rend_shadow`, `g_rendBusyUntilUs`), `core/hw/pvr/ta_ctx.cpp` (`QueueRender`) | MÉDIO | **implementado; sem ganho de fps ainda (limite físico medido)** | 2026-09-24. Medido: a emu thread espera o Process do render só **0,3ms/frame no DOA2 e 0,13ms no Zombie** (novo contador `emu_re_wait_*`) → **fila NÃO limita a velocidade de DOA2/Zombie**; eles são limitados pela emulação. KOF Evo: após o Process os dados vão para um contexto-sombra (troca de ponteiros) e o slot é liberado (`FC_NO_EARLY_RELEASE=1` desliga); sem critério, fps 36-43 → **53,6** mas velocidade 100% → **90,8%** (emu esperava 2,7ms/frame o Process do frame enfileirado atrás do render). Com critério "só enfileira se o render termina em ≤1ms" (`FC_EARLY_THRESHOLD_US`): 100,0% de velocidade, espera 0,014ms, mas fps volta a ~37. **Conclusão:** o ciclo da render thread no KOF Evo (Process 2 + Render 14,1 + apresentação ~1 ≈ 17,2ms) é maior que o intervalo do jogo (16,8ms); para ~55-60 fps a 100% o render precisa ficar mais curto (GLES: ex. um único upload de índices por DrawList em vez de `glBufferData` por lote). |
| 4.34 | **DOA2 e Zombie Revenge: o custo que sobra é a MOLDURA de blocos minúsculos no laço de vértices** | `core/rec-ARM64/rec_arm64.cpp`, `core/hw/sh4/dyna/*` (formação de blocos, regalloc por bloco) | MÉDIO (teto medido ~5%) | **confirmado (dump), não corrigido — teto estimado** | **Medição 2026-09-24 (passo 2):** classificação das instruções ARM dos 8 blocos quentes do laço do DOA2, ponderada pelo peso host: útil 40%, **grava contexto 16%, carrega contexto 13%**, prólogo 17% estático (~2 instr executadas/bloco), desvio/fim 8%, cmp/cset do T 5%. Fluxo do laço (execuções): BC4 → BE2 → (C0C 57% / BEC 43%) → … → (C38/C3A) → C4E → volta — desvios dependentes de dado, ~50/50, então uma saída lateral (superbloco) seria tomada ~metade das vezes. Teto: tráfego de contexto ~30% do laço × laço ~45% do JIT × JIT ~70% da emu thread ≈ 9% da emu thread se eliminado todo; com saídas laterais ~5%. **Caminho `pref` → TA (mesmo laço):** ~21 mil por frame no DOA2 e ~9 mil no Zombie (novo contador `ta_sq_calls`); o parser em si é barato (~11ns, só copia 32 bytes + tabela de estados), o custo é a cadeia `pref` → `blr` → `TAWriteSQ` → `ta_vtx_data32` (~50 instr/chamada ≈ 1M instr/frame ≈ ~8% do orçamento a 100%); um stub ARM64 direto economizaria ~metade (~4%). Cuidado registrado: timer local no `TAWriteSQ` ligava `__stack_chk_guard` na função inteira — medição movida para função separada. | 2026-09-24, `FC_BLOCK_PROF` + `FC_DUMP_BLOCK`. Nos dois jogos ~62-71% do tempo EMULADO é idle-loop já barato; o trabalho é o laço de vértices partido em blocos de 3-15 instr SH4 (DOA2 `8C101BC4`-`8C101C4E`, Zombie `0C0EF4B8`-`0C0EF548`), cada um pagando prólogo de ciclos, carga de todo registrador lido e gravação de todo escrito no contexto, e o `T` do `jcond` gravado e relido da memória. Ex.: `8C101C4E` 3 instr SH4 → 34 ARM. Solução: blocos maiores/traços com registradores persistentes entre blocos ligados (projeto de JIT). Menor: `jcond` usar o registrador do `seteq` direto. |
| 4.35 | **Render: cada draw em lote subia o próprio index buffer — 154 `glBufferData` por frame no KOF Evo** | `core/rend/gles/gldraw.cpp` (`DrawList`) | **ALTO** | **corrigido** | 2026-09-24. Instrumentação nova do render (`FC_REND_SPLIT`: `rs_setup/uniform_loop/upload/opaque/punch/modvol/transl/post_ms`, `rs_sort_ms`, `rs_batch_upload_ms`, `rs_batch_draw_ms`, contagem de draws simples/lote, trocas de programa/textura). KOF Evo na chuva: Render 13,6ms = **translúcidos 11,8** (sort 0,54; subida dos índices dos lotes **1,93**; draws em lote 2,84; ~334 draws simples + estado ~6,9) + opacos 1,0 + upload 0,54; laço de uniforms em todos os shaders só 0,03 (7 shaders). **Correção:** 1ª passada monta os índices de todos os draws da lista (mesma segmentação por `PP_SameGPUState`) num array, 1 `glBufferData` por lista, 2ª passada só estado + draw por offset, sem trocar de buffer (`FC_NO_ONE_UPLOAD=1` volta). **Medido (1 rodada por jogo):** Render do KOF Evo **14,1 → 10,05ms**; KOF Evo **37 → 58,8 fps a 100,0%** (p50/p95/p99 15,2/26,8/31,0ms, 40 descartes); DOA2 80,9 → **83,3%** e 33 → 43,5 fps; Zombie 74,3 → **77,9%**; Shenmue 79,9 → **81,6%**; SFA3 100%/59,8, MBAA 98,1%/50,5, KOFXI 99,0%/59,0. A velocidade subiu junto provavelmente por menos disputa de cache/memória entre render e emulação (não medido). |
| 4.36 | **Stub ARM64 direto para `pref` → FIFO da TA** | `core/rec-ARM64/rec_arm64.cpp` (`GenMemStubs`, `ta_sq_stub`), `core/hw/sh4/modules/ccn.cpp`, `core/hw/pvr/ta.cpp` (`ta_handle_cmd_ext`), `core/serialize.cpp` | BAIXO (ganho não mensurável) | **implementado, instalado** | 2026-09-24. Replica `TAWriteSQ` + `ta_thd_data32_i` em ~25 instruções sem pilha (cópia 32B com ldp/stp, tabela de estados `ta_fsm`, `ta_fsm[2048]` = estado atual); casos fora do comum (YUV/VRAM, `ta_ctx` nulo, overflow) saltam para o C. `do_sqw_nommu` (vive no Sh4RCB) volta para o C no reset do cache e aponta para o stub quando ele é gerado; **savestate:** o stub é gravado como `TAWriteSQ` (i=2, arquivo idêntico) e o load aponta para o stub se existir (antes o load forçava o C — o DOA2 nunca reescreve QACR na luta e ficava no C). Verificado: 0 chamadas ao C no DOA2 após o fix. **Medido (1 rodada):** DOA2 82,3-83,5% (sem ganho, dentro do ruído — a cadeia C era mais barata que a estimativa de ~4%), Zombie 77,9 → 80,4% (pode ser ruído), Shenmue 81,6 → 82,2%, KOF Evo 100%/59 fps; 2D iguais. MBAA manda ~1M escritas à TA sem lista iniciada (`ta_ctx` nulo) — vão ao C, que descarta (comportamento original). `FC_NO_TA_SQ_STUB=1` desliga. |
| 4.37 | **Render: margem restante medida — draw no driver Mali domina; quebras de lote são trocas REAIS de textura** | `core/rend/gles/gldraw.cpp` (`SetGPState`, `DrawList`) | BAIXO-MÉDIO | **medido, não corrigido** | 2026-09-24, contador de hardware `cntvct_el0` por draw. Quebras por textura com resto do estado igual: KOF Evo 191/frame, DOA2 20, Shenmue 39 — **mesma textura GL: 5,9 / 0 / 0** → relaxar a comparação de tcw não rende. Por frame: `SetGPState` 2,1 / 2,6 / 2,8ms (~4-5µs/draw) e **draw no driver 6,7 / 7,6 / 7,5ms (~12-14µs/draw, 70%)**. DOA2 e Shenmue: opacos 7,8 e 6,8ms do render. Alavancas restantes: (a) cache de `glUniform1f/1i` e da busca de programa (~1ms estimado); (b) menos draws — reordenar a lista opaca por textura (risco em coplanares, cuja ordem decide empates de profundidade) ou atlas/array de texturas (projeto grande). Hoje o render não limita KOF Evo (59 fps) nem DOA2/Shenmue (limitados pela emulação). |
| 4.38 | **Laços de espera de DOA2 e Shenmue só eram "cobrados" pelo `idle_hash` antigo, não pulados — e o `idle_hash` tem hash de metade do bloco em bytes** | `core/hw/sh4/dyna/decoder.cpp` (`idle_hash`, detector de laço pequeno, `idle_ff_sigs`), `core/hw/sh4/dyna/driver.cpp` (`RuntimeBlockInfo::hash`) | ALTO (diagnóstico) / MÉDIO (ganho) | **avanço até o evento implementado para DOA2 e Shenmue; hash não corrigido** | 2026-09-24, `FC_IDLE_LOG` (novo) + `FC_BLOCK_PROF`. O `idle_hash` responde por 62% (DOA2), 50% (Shenmue) e 42% (KOF Evo) do tempo emulado, mas só cobra 224 ciclos por bloco: o laço de espera continuava girando (DOA2 184 mil voltas/s emulado, ~286 mil `UpdateSystem`/s). **DOA2** (8C12F99E): `while (*flag == r14) { (*contador)--; tarefa(); }`, tarefa vazia; **Shenmue** (0C03A1F8): `do { tarefa(); compara; } while (*flag != 0)`, tarefa de 2-11 instr. Assinaturas `doa2-wait-flag-loop` e `shenmue-wait-flag-loop` no mecanismo do kofxi (pula até 1 ciclo antes do próximo evento; `FC_IDLE_FF=0` desliga). **Medido (1 rodada cada lado, mesmo binário):** DOA2 84,8 → **87,6%** (underruns 79 → 66); Shenmue 80,7 → **83,1%**, 24,2 → 24,9 fps, p50/p95/p99 41,0/43,4/50,7 → 39,8/42,1/49,6ms. Nenhuma assinatura casa em Zombie/KOF Evo/Shenmue↔DOA2; 2D iguais. **Contador do DOA2:** já descia ~17× menos que no hardware (690 ciclos cobrados por volta contra ~40 reais) sem efeito visível. **Bug do hash (não corrigido):** `hash()` lê `ptr` como `u8*` só nos primeiros `guest_opcodes` bytes (metade do bloco) e o teste de load relativo ao PC nunca dispara — a entrada `>:1:04:B806EEE4` ("very suspicious" no próprio código) casa 11 blocos genéricos no Shenmue (0,6% do tempo emulado lá; efeito pequeno). Detector de laço pequeno marca ~1000 blocos no DOA2 mas infla só 2-5% do tempo emulado. **Conclusão sobre o "eureka" do DOA2:** com a espera de graça, a conta fecha — o trabalho real executa ~116M instr SH4 por segundo emulado (o SH4 real faz ~1,6 instr/ciclo), e o JIT custa ~11 ciclos ARM por instrução (5,5 instr × IPC 0,5) ≈ o núcleo inteiro; + som (`AicaUpdate`, novo contador por dispositivo no scheduler) ~2,7ms/frame. O que falta para 100% é o custo por instrução do JIT (tráfego de contexto entre blocos, `ftrv` recarregando XMTRX, blocos pequenos). |
| 4.39 | **`sh4clock` padrão `d12` multiplicava os ciclos de todo bloco por 1,2 — SH4 emulado ~20% mais lento que o real (~167MHz efetivos)** | `core/libretro/libretro.cpp` (`_sh4clock`), `core/hw/sh4/dyna/decoder.cpp` (`guest_cycles *= sh4clock`); cfg `flycast2026_sh4clock` | **ALTO** (fidelidade; destrava jogos limitados em tempo emulado) | **corrigido no cfg (`d10`), laço de atraso do Le Mans pendente** | 2026-09-24, "método Demóstenes" com o Le Mans 24h: velocidade 100% mas o jogo pede só **16,1 frames/s em tempo emulado** e o render espera 45ms/frame pela emulação. `FC_BLOCK_PROF`: **um bloco de 7 instr (8C1730F6) = 54% do tempo emulado e 69% do trabalho do JIT**, 12,6M voltas/s — laço de ATRASO por contagem (`while (r4--) contador++`, ~787 mil voltas por frame ≈ 31ms emulados a 8 ciclos/volta). **Teste do clock (1 rodada cada):** d12 16,1 frames/s do jogo, 100%; **d10 18,8, 100%** (p50/p95/p99 56,3/83,3/88,0 → 50,8/70,1/81,2ms); d8 25,3 mas 83,7% (host satura girando o laço). **d10 nos outros:** DOA2 88,7% (d12 87,6), Zombie 81,2 (80,6), Shenmue 82,8 (83,1), KOF Evo 100%/57,6 fps, SFA3 98,9%, MBAA 98,1% (jogo pede 54,9 x 54,0), KOFXI 99,8% — sem perda, porque onde a espera é pulada (ou cobrada por valor fixo do `idle_hash`) o SH4 mais rápido só aumenta tempo ocioso. **Aplicado:** `flycast2026_sh4clock = d10` em `~/.config/retrorun.cfg` e `retroarch-core-options.cfg` (backups `*.bak-pre-sh4clock-*`). O modelo base ainda cobra ~1 ciclo por instrução (o SH4 real faz até 2), então d10 ainda é conservador. **Próximo:** pular o laço de atraso do Le Mans (somar as voltas ao contador e cobrar os ciclos, até o próximo evento) — libera ~69% do JIT dele e permitiria clock nominal real (d8) sem saturar. |
| 4.40 | **Pulo do laço de atraso por contagem (Le Mans)** | `core/hw/sh4/dyna/decoder.cpp` (`delay_loop_match`), `core/hw/sh4/sh4_sched.cpp` (`sh4_delay_loop_skip`), `core/rec-ARM64/rec_arm64.cpp`, `blockmanager.h` (`delay_skip`) | ALTO (Le Mans) | **implementado, instalado** | 2026-09-24. Padrão exato `mov.l @(disp,PC),r1 / mov.l @r1,r2 / add #1,r2 / mov.l r2,@r1 / tst r4,r4 / bf.s (volta ao bloco) / add #-1,r4` = `while (r4-- != 0) (*contador)++`. Na entrada do bloco (antes do regalloc) pula k voltas: `contador += k`, `r4 -= k`, `sh4_sched_next -= k*ciclos`, com k limitado ao próximo evento e deixando ≥1 volta real para a saída normal (estado final idêntico). `FC_NO_DELAY_SKIP=1` desliga; contadores `delay_skip_*` no dump do `FC_IDLE_FF_STATS`. **Medido (1 rodada):** ~845 mil voltas puladas por frame; Le Mans d10 18,6 frames/s (igual, não era limitado pelo host), **d8 25,4 frames/s a 100%** (antes 83,7%; p50/p95/p99 42,4/54,8/66,7ms contra 56,3/83,3/88,0 no d12 original). Não dispara em DOA2/Zombie/Shenmue/KOF Evo/SFA3/MBAA/KOFXI (todos iguais). Padrão do cfg segue `d10` (equivale ao clock nominal do upstream); `d8` dá ao SH4 emulado mais que o modelo base (que cobra ~1 ciclo/instr, o real faz até 2) — candidato a padrão após teste em mais jogos. |
| 4.41 | **Detector de "laço pequeno" tratava varredura (`strlen`) como espera e cobrava ×30 os ciclos** | `core/hw/sh4/dyna/decoder.cpp` (detector small/self) | **ALTO** (fidelidade) | **corrigido, instalado** | 2026-09-24. No Le Mans, `8C0112F8` (`add #1,r5; mov.b @r4+,r3; tst r3,r3; bf` = strlen) era marcado small (×3) + self (×10): 144 ciclos por volta em vez de ~5 → **25% do tempo emulado era ciclo inventado em trabalho real**. Critério novo: só é espera se o endereço de toda leitura do bloco NÃO é escrito dentro do bloco (laço de espera relê o mesmo endereço; varredura avança o ponteiro). `FC_IDLE_SCAN_OLD=1` volta ao antigo. **Medido (1 rodada, d10):** Le Mans **18,6 → 24,2 frames/s do jogo** a 99,9%, p50/p95/p99 47,7/88,8/91,0 → 43,1/57,0/67,6ms (≈ o d8 sem mexer no clock); **Zombie 81-84 → 87,5%** e 52 fps; MBAA pede 55,7 frames/s (antes 54-55); DOA2 empate em 2 rodadas lado a lado (87,1 x 87,8%); Shenmue, KOF Evo, SFA3, KOFXI iguais. |
| 4.42 | **Clock do SH4 por jogo (`lut_games.sh4clock`) — Le Mans a 0.8** | `core/rom_luts.h` (campo `sh4clock`, entrada `T15111D 50`), `core/nullDC.cpp` (`LoadSpecialSettings` → `g_lutSh4Clock`), `core/hw/sh4/dyna/decoder.cpp` | MÉDIO | **implementado, instalado** | 2026-09-24. d8 global: Le Mans +, mas DOA2 88,6 → 82,2% e Shenmue 83,4 → 78,3% (esperas não puladas giram mais). Jogos que usam o tempo livre fazendo trabalho (Le Mans: contagem) ganham com mais CPU emulada; jogos que esperam perdem. Override por código de produto, aplicado na compilação dos blocos (a opção do core não o desfaz). **Medido:** Le Mans com cfg d10 → **30,1 frames/s (pede 29,96), 100,5%**, p50/p95/p99 33,1/36,0/43,2ms; DOA2 88,5% (inalterado). Alternativa futura: `sh4clock` dinâmico (mais CPU quando o jogo não tem tempo ocioso e o host tem folga). |
| 4.43 | **Shenmue II: laço de espera (mesmo do Shenmue 1, outra compilação) + espera por contador em memória — avanço até o evento** | `core/hw/sh4/dyna/decoder.cpp` (`shenmue2-wait-flag-loop`, `poll-until-counter-r1-r4`, campo `ram_reg`), `core/hw/sh4/sh4_sched.cpp` (`sh4_sched_idle_fastforward_if_ram`), `blockmanager.h` (`idle_ff_ram_reg`), `rec_arm64.cpp` | **ALTO** | **implementado, instalado** | 2026-09-24, primeira rodada do Shenmue II (save do usuário, cena a ~20 fps): 68,6%, 20,6 fps, jogo pede 30; emulação 50ms/frame, render 14,7ms com 30ms de folga. **320×240: idêntico (67,8%, render 14,6ms) → não é GPU.** `FC_BLOCK_PROF`: laço de 4 blocos (8C209F44 chama tarefa VAZIA 8C20F060, 8C209F4C compara, 8C209F5A testa o flag) = **54% do trabalho do JIT**, ~50% do tempo emulado; + 8C053F7A (`mov.l @r1,r0; cmp/hi r0,r4; bt` = espera contador em memória, 27% do tempo emulado, era small+self ×30). Assinaturas de avanço até o evento para os dois. **Cuidado adicionado:** a assinatura curta só avança se `r1` aponta para a RAM principal (registrador de hardware como o TCNT muda continuamente e o avanço passaria do ponto) — contador `ff_skipped_hw_per_frame`; nos jogos testados sempre RAM. **Medido (1 rodada cada lado):** Shenmue II **68,4 → 84,2%**, 20,5 → 25,0 fps, p50/p99 47,9/99,0 → 37,6/92,1ms, underruns 146 → 59; a assinatura curta também casa no **Zombie: 80,8 → 87,0%** (51,9 fps, underruns 100 → 73), SFA3 (99,9%) e KOF Evo (100%); Shenmue 1, DOA2, Le Mans (30 fps), MBAA, KOFXI iguais. |
| 4.44 | **Mixagem das 64 vozes do AICA na emu thread (~10ms/frame no save pesado do Shenmue II) — render em thread própria + bloco rápido de controle** | `core/hw/aica/sgc_if.cpp` (`ControlStep`, `FastEligible`/`FastControlBlock`, `RBlock`, `aica_render_block`, fila), `core/hw/aica/aica.cpp`, `core/serialize.cpp`, `core/libretro/audiostream.cpp` (`FC_AUDIO_DUMP`) | **ALTO** | **implementado, instalado** | 2026-09-24/25, save novo do Shenmue II (56,5%, 17 fps, frame emulado 63ms, render 17ms). `FC_REND_SPLIT`: `AicaUpdate` = 13,2ms/frame (ARM7 2,8 + mixagem 10,4); 72 mil passos de voz por frame (~49 de 64 vozes ativas, 89% ADPCM, 68% em release, 66% com filtro), ~220 ciclos por passo. **1ª tentativa (mixagem inteira em thread, sincronizando nos acessos): nulo** — o driver do ARM7 lê os registradores de monitor (CA/EG/LP) e a palavra 0 dos canais (KYONB, que a própria mixagem limpa quando a amostra acaba) ~60×/frame, quase todo bloco; e 58% do tempo emulado é avanço até o evento, sem trabalho do SH4 para esconder a mixagem. Além disso ler KYONB atrasado + escrever de volta redisparia notas. **Desenho final (semântica exata):** a emu thread faz o CONTROLE (envelopes, LFO, posição, loop, decodificar ADPCM lendo a RAM de som no mesmo instante) e grava por amostra só os insumos do render (s0/s1, fp, AEG, FEG, ALFO; + CDDA e volume master por bloco); a thread de som faz interpolação, filtro, volume, mixagem e entrega do áudio (nada disso realimenta o estado emulado → fila de até `FC_AICA_DEPTH`=24 blocos, só aumenta latência de áudio). Estado do filtro (prev1/prev2) é da thread de render, sincronizado no savestate (`aica_mix_sync`). Thread dorme 1ms quando a fila esvazia (acordar por futex a cada bloco custava ~1ms/frame na emu thread). **Bloco rápido de controle:** quando dá para provar no início do bloco que em 32 amostras não há transição de AEG/FEG, tick de LFO que mude algo, nem fim/loop da amostra (86% dos blocos), um laço sem chamadas indiretas faz a mesma aritmética (controle 7,4 → 5,2ms). `FC_AICA_THREAD=0` render síncrono (mesmo código); `FC_AICA_FAST_CTRL=0` só o caminho por amostra. **Validação:** PCM gerado (`FC_AUDIO_DUMP`) **bit a bit idêntico ao original** em 14,4s do Shenmue II, nos modos síncrono, thread e com bloco rápido. **Medido (1 rodada):** Shenmue II save novo **56,5 → 60,6%** (17,0 → 18,2 fps, frame emulado 63,2 → 59,3ms, p50/p95/p99 52,4/81,6/83,3 → 48,3/77,4/78,3ms); Shenmue 1 **86,5%** (antes ~83); **Zombie 100,2%** (antes ~87); DOA2 88,1% (igual); KOF Evo 53,0 fps a 99,9% e Le Mans 97,0% (base instalada na mesma rodada: 55,2 fps/100,1% e 97,8% — ruído, KOF Evo a conferir jogando); SFA3 100,1%, MBAA 100,3%, KOFXI 100,1%. **Lição de instrumentação:** ler `cntvct_el0` custa ~160ns neste SoC — contador dentro de laço quente distorce a medida (usar só por bloco/callback). Restante no Shenmue II: ARM7 2,9ms + controle 5,2ms na emu thread; JIT do SH4 ~50ms/frame com perfil plano (laço de transformação de vértices em 8C1D8BDA-8C1D8C84 ~20%, picado em ~7 blocos por vértice por `bf +0` que só pula 1 instrução). |
| 4.45 | **Estudo completo do código gerado pelo JIT (4 jogos): 44-60% das instruções do host são overhead do flycast** | `docs/jit_study.md`, `tools/jit_study.py`, `FC_JIT_DUMP` em `core/rec-ARM64/rec_arm64.cpp` / `blockmanager.cpp` / `driver.cpp` | **ALTO** (base do `jit_armv8_a`) | **confirmado (medido)** | 2026-09-25. Dump de todo bloco compilado (SH4, SHIL com trecho ARM64 por op, bytes, execuções, faults por região, limpezas de cache). Shenmue II / DOA2 / Shenmue 1 / MBAA: 3,6-4,1 instr ARM64 por instr SH4; carga+descarga de registrador **26-28%** (alocador write-through por bloco, sem registrador entre blocos); saída 11-21%; entrada 6-10%; blocos de 5,6-9 instr SH4. **Maior tráfego com o contexto: `sr.T` e `jdyn`** (T gravado a cada comparação, `jcond` copia para `jdyn`, saída relê: 94-207 M recargas). `ftrv` 13 instr (recarrega XMTRX; conta = 4), `pref` 12 instr + chamada (9,3% do host no DOA2). Memória: RAM 83-98% dos acessos a ~2 instr (fastmem ok), vídeo via SQ 1,5-24%, **som ~0** (o SH4 não toca no AICA), sistema <0,5%. **Cache de código não estoura** (2 MB de 15; limpezas só no boot/savestate); **o L1I estoura**: Shenmue II precisa de 119 KB de código para 80% da execução (DOA2/Shenmue 1/MBAA 31-39 KB). Shenmue 1: 4108 escritas em página de código. Implicações (registradores fixos, T em registrador, superblocos, densidade) em `docs/jit_study.md`. |
| 4.46 | **Tabela de despacho FPCB (128MB, páginas de 4KB) e desvio indireto das saídas dinâmicas** | `core/rec-ARM64/rec_arm64.cpp` (`RelinkBlock`, BET_Dynamic*; `jit_dump_dyn`), `core/hw/sh4/sh4_if.h` (`FPCB_SIZE`), `tools/fpcb_bench.c`, `tools/dispatch_branch_bench.c`, `tools/jit_dyn_offsets.py` | BAIXO-MÉDIO | **medido; hipótese "tabela cara" descartada** | 2026-09-25, Shenmue II: ~1,2M saídas dinâmicas por segundo real (82% `rts`, 17% `jsr @Rn`), 4.903 destinos, 127 páginas da tabela para 90%. Replay da sequência real no device: tabela 128MB × compacta = +5,7ns isolado, +8,5-16ns com 16 leituras de RAM concorrentes; desvio indireto com destinos reais = +10ns de previsão errada (+3,3ns de layout). Total evitável ~20-30ns/saída ≈ **2,5-3,5% da emu thread** (limite inferior: microbenchmark isolado); a tabela em si é ~1-2%. Alvo para o `jit_armv8_a`: retorno previsível (`jsr`/`rts` como `bl`/`ret` do host), não trocar a tabela. Device sem THP (tudo 4KB). Detalhes em `docs/jit_study.md`. |
| 4.47 | **Ciclos da emu thread (PMU): 21% esperando instrução (L1I), 13-18% esperando load; ~15% do Shenmue II é descompressão de CHD na emu thread** | `tools/pmu_game.sh`; `core/deps/lzma`, `core/imgread/chd.cpp` (CHD na emu thread) | **ALTO** | **medido** | 2026-09-25. Shenmue II / DOA2: IPC 0,43/0,44 a ~1,27GHz reais; eventos de parada do A53 (0xE0-0xE8): fila vazia por L1I 21,0/19,1%, espera de load 17,6/13,0%, previsão de desvio etc. 8,2/6,0%, dependências 13/18%. 83%/67% das paradas de L1I e 72%/49% das de load no código do JIT; no tempo do JIT do Shenmue II ~26% espera instrução, ~19% espera dado, ~55% executa. **Achado fora do JIT: `LzmaDec_DecodeReal2` 9,7% + `ecc_compute_bytes` 4,2% + `crc16` 0,9% = ~15% dos ciclos da emu thread no Shenmue II (leitura de disco contínua na cena); DOA2 ~1%.** Candidatos: ler/descomprimir o CHD numa thread à parte (prefetch de hunks), ou não regenerar ECC de setores de dados. |
| 4.48 | **Emulação determinística a partir do savestate — ferramenta de validação de JIT** | `core/hw/pvr/Renderer_if.cpp` (`FC_STATE_HASH`), `core/hw/aica/aica_if.cpp` (`FC_RTC_FIXED`), `core/libretro/libretro.cpp` (`FC_INPUT_NEUTRAL`), `core/libretro/audiostream.cpp`, `core/serialize.cpp`, `tools/state_compare.py` | **ALTO** (base de teste do `jit_armv8_a`) | **implementado, confirmado** | 2026-09-25. A única fonte de variação entre rodadas era o **RTC, que não entra no savestate** (começa na hora do host no boot; DOA2 diverge no frame 5, Shenmue II muda 1 byte da RAM). Com `FC_RTC_FIXED`, Shenmue II, DOA2 e MBAA saem idênticos em duas rodadas (TA de todo frame, RAM/VRAM/ARAM/contexto a cada 30 frames, PCM), com e sem render em thread. Sensibilidade: `FC_NO_ZX_LOAD=1` idêntico, `FC_IDLE_FF=0` diferente. Hashes com o render em thread também saem iguais: a liberação antecipada e o descarte de frames não mudam o estado emulado. |
| 4.49 | **Desvios e chamadas do guest: `rts` volta ao endereço empilhado 98,8-99,97%; 19-44% das saídas condicionais pulam ≤4 instruções** | `core/rec-ARM64/rec_arm64.cpp` (contadores de saída condicional e pilha-sombra, só com `FC_JIT_DUMP`), `core/hw/sh4/dyna/blockmanager.cpp` (linhas C), `tools/jit_branch_study.py` | ALTO (desenho do `jit_armv8_a`) | **medido** | 2026-09-25, Shenmue II / DOA2 / Shenmue 1 / MBAA. Pilha-sombra: `rts` confere com o `NextBlock` do `bsr`/`jsr` em 99,97 / 99,70 / 98,75 / 99,80% (Shenmue 1 enche a pilha: chamadas sem retorno, troca de tarefa). Saídas condicionais: salto curto para frente 33/27/44/19%, laço 17/11/28/48%, lado dominante ≥90% em 58/35/67/86% das execuções. Retorno como `ret` do host e execução condicional para saltos curtos são as alavancas. |
| 4.50 | **Protótipo do `jit_armv8_a` no laço de vértices do Shenmue II: 1,46-1,70× mais rápido, código 3,8× menor, resultado idêntico** | `tools/proto_jit_armv8_a/` (`new.S`, `gen_cur.py`, `harness.c`), `FC_CAPTURE_BLOCK` em `core/rec-ARM64/rec_arm64.cpp` | **ALTO** | **medido** | 2026-09-25. 11 blocos (`8C1D8BDA`-`8C1D8C82`, ~20% do JIT do Shenmue II), estado real capturado na entrada do laço. Versão atual = código do dump com saídas ligadas; versão nova escrita à mão (registradores fixos, T em registrador, `bf +0`/`bt.s` curto como `csel`/`fcsel`, XMTRX em NEON, uma checagem de ciclos por volta). Registradores, T, FPUL, SQ e dados do TA idênticos em 2 estados. Strip de 14 vértices 3036 → 1781 ns (1,70×), de 2 vértices 322 → 220 ns (1,46×); 2740 → 724 bytes de código. Cache quente: não inclui o ganho de L1I (~26% do tempo do JIT no jogo) nem os trampolins da SQ que o código atual paga no emulador. |
| 4.51 | **`jit_armv8_a` v0 (esqueleto correto): backend novo, selecionável, IDÊNTICO ao JIT atual nos 3 jogos — ainda mais lento** | `core/rec-ARM64/jit_armv8_a.cpp`/`.h` (novo), `core/rec-ARM64/rec_arm64.cpp` (dispatch + `FC_JIT_TRACE`), `core/hw/aica/aica_if.cpp` (`GetRTC_now`), `core/hw/pvr/Renderer_if.cpp` (`FC_STATE_HASH_CTXDUMP`, `jdyn` fora do hash), `Makefile.common` | **ALTO** (base do `jit_armv8_a`) | **v0 implementada e validada (correta); ganho de velocidade pendente** | 2026-09-25. **Desenho v0:** arquivo à parte, selecionado por `FC_JIT_ARMV8_A=1` e só com `!mmu_enabled()` (com MMU, passa pro backend antigo). r0-r7 do SH4 fixos em x19-x26 (callee-saved: sobrevivem a chamada C++ e ao `intc_sched` do mainloop); o resto do banco fica no `Sh4Context`. Bloco carrega r0-r7 no prólogo e grava antes de toda saída. Nativas: ALU/deslocamento/extensão/teste/setcc, `mov32`/`mov64`, `jdyn`/`jcond`, `ifb`, FPU (NEON, mesmas instruções do backend antigo — o fallback canônico C++ diverge em FMA/`fixNaN`), `fpscr` (com a guarda PR/SZ), leitura/escrita de memória por chamada C++. O resto cai em `shil_chf` (canônico C++), com flush/reload dos fixos. Mainloop compartilhado (x28=contexto, w27=ciclos, w29=next_pc). **Validação (a obrigatória):** DOA2, Shenmue II e MBAA **IDÊNTICOS** (TA de todo frame, RAM/VRAM/ARAM/contexto, PCM) contra o backend antigo, mesmo savestate, `FC_RTC_FIXED=600000000 FC_INPUT_NEUTRAL=1 FC_STATE_HASH FC_AUDIO_DUMP` + `tools/state_compare.py`; o backend novo também é auto-determinístico (2 rodadas idênticas). **Velocidade (retrorun, 1 rodada, 30s/10s):** ainda MUITO mais lento que o antigo — DOA2 89,3%→40,8%, Shenmue II 60,5%→24,5%, MBAA 100,1%→86,8%. Esperado: memória por chamada C++, `shil_chf` para muitas ops e flush/reload em toda escrita. **Bugs achados e corrigidos na validação:** (1) FPU canônica divergia do NEON do backend antigo → FPU nativa; (2) `jdyn` não era gravado pelo `jcond`/`jdyn` (o backend antigo dependia do `Cleanup()` do regalloc) → gravado explicitamente; (3) guarda PR/SZ ignorada: bloco que muda PR/SZ no meio não retomava em `rs2` → implementada igual à do backend antigo; (4) `writem` por `WriteMem32` desprotegia página de código que o backend antigo mantém protegida (divergência de SMC) → escrita via `bm_WriteMemCodePageN`; (5) os fixos não iam ao contexto antes de escrita, então `FC_STATE_HASH` (lido no meio do bloco) via r4 velho → flush antes de escrita. **Harness:** `FC_RTC_FIXED` agora também fixa `GetRTC_now()` (`FixUpFlash` usava a hora do host e quebrava o determinismo entre rodadas de ~13s); `jdyn` (scratch interno do dynarec, não registrador do SH4) sai do hash de contexto. **Próximos passos:** fastmem + trampolins de fault (item 4.27), T em registrador e `csel` para `bf/bt` curtos, blocos maiores, `jsr`/`bsr` como `bl` e `rts` como `ret`. |
| 4.52 | **`jit_armv8_a` passo 1: fastmem — velocidade quase dobra, continua IDÊNTICO** | `core/rec-ARM64/jit_armv8_a.cpp` (`GenReadMemory`/`GenWriteMemory`, `CompactUsable`, `EnsureMemBase`), `core/rec-ARM64/rec_arm64.cpp` (`GenCompactTrampoline`) | **ALTO** | **implementado, validado** | 2026-09-25. Leitura/escrita viram `ldr/str [x13, wAddr, uxtw]` (mesma codificação do backend antigo; x13 = contexto+0x1c0; endereço no fixo direto ou em w14; dado no fixo ou w15/x15). Fora da RAM o acesso dá fault uma vez e `ngen_Rewrite` troca o site pelo trampolim de sempre (MMIO, SQ, OCRAM, página de código protegida). A descarga de r0-r7 antes de toda escrita saiu do caminho rápido: no modo `jit_armv8_a` o trampolim genérico grava w19-w26 no contexto antes de chamar o C++ (handler de MMIO pode ler o contexto: pedido de render, `FC_STATE_HASH`). `FC_NO_COMPACT_MEM=1` volta à chamada C++. **Validação:** Shenmue II, DOA2, MBAA **IDÊNTICOS** ao backend antigo (`state_compare.py`, TA/RAM/VRAM/ARAM/contexto/PCM); sem fault genuíno. **Velocidade (1 rodada, v0 → fastmem, antigo entre parênteses):** Shenmue II 24,6 → **47,5%** (60,5), DOA2 41,1 → **65,9%** (86,8), MBAA 79,2 → **100,0%** (100,0). Próximo: T em registrador e `csel` para `bf/bt` curtos; fixar o resto do banco (fr, T, pr) para cortar carga/descarga de contexto. |
| 4.53 | **`jit_armv8_a` passos 2-5: cache de FP (v16-v31), entrada fria/quente, cache de GPR (w2-w8), `pref` nativo — Shenmue II parado em ~47%; causa: código 2× maior que o do JIT antigo (L1I/L2)** | `core/rec-ARM64/jit_armv8_a.cpp` (`FSlot`/`FRd`/`FWr`/`FRecordSite`, `GSlot`/`GRd`/`GWr`, `LoadPinned`/`StorePinned`, `GenLinkStubs`, `shop_pref`), `core/rec-ARM64/rec_arm64.cpp` (`GenCompactTrampoline` grava fixos e spills por site, `EmitPerfMapEntry`) | **ALTO** | **implementado, validado; diagnóstico medido** | 2026-09-24. Todos IDÊNTICOS ao backend antigo (`state_compare.py`), 0 fault. **Velocidade (1 rodada; Shenmue II / DOA2):** fastmem 47,5 / 65,9% → cache de FP 47,1 / 64,8 → entrada quente 46,8 / 66,1 → cache de GPR 47,3 / 69,2 → `pref` nativo 47,8 / **71,7%** (antigo 60,5 / 86,8). Cache de FP e entrada quente foram hipóteses sem medição prévia e deram zero. **Diagnóstico (perf record da emu thread + `FC_PERF_MAP=1`, amostras mapeadas à mão no `/tmp/perf-PID.map` porque o cache de código fica dentro do `.so` e o perf atribui tudo a `SH4_TCB`; normalizado pelo trabalho emulado):** tempo em blocos 1,39× o do JIT antigo, fora dos blocos igual; o excesso é difuso (os 30 blocos com mais excesso explicam só 2078 de 11737 amostras); blocos 1,5-2,2× maiores; **código que cobre 80% das amostras: 266 KB (antigo) × 520 KB (novo)**, contra L1I de 32 KB e L2 de 256 KB. Nos dumps (`FC_DUMP_BLOCK`), ~1/3 das instruções eram carga/descarga de r0-r7: 8 `ldr` de entrada fria + 16 em volta do `UpdateSystem` inline em todo bloco, e 16 em volta de cada chamada C++; par de float ↔ memória em 4 instruções; constante float por `mov`+`fmov`. |
| 4.54 | **Regiões quentes: DOA2 tem ~45% do custo do JIT num laço de 10 blocos (vértices → Store Queue); Shenmue II, 18 regiões dão 50%** | `tools/region_study.py` (novo), dumps `FC_JIT_DUMP` | **ALTO** (alvo do nível 2) | **medido (offline, dump do JIT antigo)** | 2026-09-24. Grafo de blocos com peso nas arestas (contadores `C` + desvios estáticos do `G`), blocos quentes (90% do custo, execuções × instr ARM) agrupados por aresta dominante. DOA2: 1 região = 52% do custo, laço interno 8C101BC4-8C101C4E com 10,6 M voltas ≈ 45%; Shenmue II: 18 / 183 regiões = 50 / 80%, a maior (24%) é o laço de vértices do protótipo (4.50). 96-97% das transições saindo de bloco quente ficam dentro da própria região, isto é, o custo de fronteira de bloco (contexto, ciclos, ligação) que o nível 2 elimina. Detalhe em `docs/jit_study.md`, seção "Regiões quentes". |
| 4.55 | **Região do DOA2 escrita à mão (nível 2): laço de vértices 1,76-1,85× mais rápido, código 3,5× menor, IDÊNTICO incluindo ciclos; protótipo do Shenmue tinha ciclos errados** | `tools/proto_jit_armv8_a/doa2/new.S`, `extract_blocks.py` (novo), `gen_cur.py`/`harness.c` (parametrizados; harness compara ciclos, contexto inteiro e RAM) | **ALTO** | **medido (harness, cache quente)** | 2026-09-24. 12 blocos 8C101BC2-8C101C4E (~45% do custo do JIT do DOA2, 4.54), estado real capturado no início da strip. Strip de 18 vértices 4625 → 2499 ns (1,85×), de 5 vértices 1167 → 662 ns (1,76×); código total 2000 → 960 bytes (2,1×), caminho quente ~1616 → 564 (~2,9×; primeira versão do registro dizia 3,5× comparando o quente do novo com o total do atual). Nove regras explícitas no README (guarda de ciclos por ponto de entrada com caminho máximo e subtração por bloco original, SH4 vivo em callee-saved, T como desvio, registrador derivado, stores agrupados com guarda de wrap, descarga da SQ como chamada C completa, blocos equivalentes compartilhados, `pref` com SQ fora da linha). **Shenmue (4.50):** com o harness novo o `new.S` dele usa 602 ciclos contra 257 do JIT atual — a checagem única de 86 por volta não era a soma dos blocos; TA/SQ/registradores seguem iguais. Refazer com a regra de ciclos. |
| 4.56 | **Gerador offline do nível 2 (v1): regiões do DOA2 e do Shenmue II geradas do SHIL, IDÊNTICAS incluindo ciclos, 1,53-1,63× (DOA2) e 1,40× (Shenmue II)** | `tools/tier2_gen.py` (novo) | **ALTO** | **medido (harness, cache quente)** | 2026-09-24. Entrada: dump + lista de blocos; saída: `.S` com a interface do harness. v1 com as regras estruturais (guardas de ciclos com a da volta subindo para antes da chamada, alocação callee/caller-saved, liveness em volta da descarga da SQ, `pref` subindo, layout pelo bloco mais executado). DOA2: 4558 → 2797 ns (18 vértices), 1179 → 772 (5); caminho quente ~1616 → 808 bytes (à mão: 564, 1,85×). Shenmue II, mesmo gerador sem mudança: 3087 → 2198 ns, ciclos 257 = 257 (o protótipo à mão tinha 602). Falta para chegar à versão à mão: T fundido em flags, stores agrupados, registrador derivado, fpul limpo no contexto depois da chamada, reload de só-lidos raros sob demanda, blocos equivalentes compartilhados. |
| 4.57 | **Nível 2 no emulador (experimento): região do DOA2 gerada offline e injetada no JIT antigo — jogo inteiro IDÊNTICO, velocidade do jogo 86 → 95%; fps apresentado cai ~7% (render vira o gargalo)** | `core/rec-ARM64/tier2_doa2.S` (gerado por `tools/tier2_gen.py --emu doa2`), `core/rec-ARM64/rec_arm64.cpp` (`FC_TIER2`, gancho no `ngen_Compile`, `t2_no_update`, fault na região recusado), `Makefile.common` | **ALTO** | **medido (jogo, 2 rodadas por lado)** | 2026-09-24. `FC_TIER2=1`: os blocos 8C101BC2/8C101BC4 do JIT antigo começam com `b` para a região (se o SH4 dos 12 blocos na RAM bate com a tabela gerada e a página é protegida); guarda de entrada falhando volta para o resto do bloco antigo; saídas pelo despachante; descarga da SQ pelo `do_sqw` real; stores na SQ direto no `sq_buffer` (checagem no início do bloco). Gerador: modo `--emu` (ciclos em w27, base em x13) e stores agrupados (harness 1,63 → 1,74×). **Equivalência (`state_compare`, mesmo savestate, antigo × antigo+região):** IDÊNTICO — 1102 frames do TA, 18,4 s de áudio; a rodada com região fez 347 frames a mais no mesmo tempo. **Velocidade (retrorun, 2 rodadas por lado, mesmo core):** velocidade do jogo 85,9/86,9 → **94,0/95,9%**, intervalo do frame emulado 20,5/20,4 → 18,8/18,3 ms, underruns 70/67 → 35/24; fps apresentado 33,3/34,3 → 31,9/31,6, frame time médio 30,0/29,1 → 31,3/31,6 ms, p50 33,0/30,2 → 32,6/32,7, p95 41,8/39,9 → 40,1/39,8, p99 54,3/50,9 → 54,0/51,6. O DOA2 é limitado pelo render (4.20, ~640 draw calls): emulando mais rápido, a fila de render descarta de outro jeito (família do 4.29). **Avaliação jogando (usuário): o mais jogável até agora, sem os pequenos stutters que existiam mesmo com mais fps apresentado** — a queda de fps apresentado não é perda percebida (`docs/game_status.md`). Limites do experimento: região fixa (DOA2), fault dentro dela para o core com mensagem (assume RAM/SQ), leitura de par de floats sem forma compacta. |
| 4.58 | **Nível 2 em tempo de execução, etapa 1: gerador em C++/VIXL (`tier2.cpp`) compila a região do `oplist` dos blocos já compilados e liga por patch do bloco antigo — IDÊNTICO, igual ao `.S` offline** | `core/rec-ARM64/tier2.cpp` (novo), `driver.cpp` (`tier2_code_reserve` no `emit_FreeSpace`), `sh4_interpreter.cpp` (`UpdateSystem` → `tier2_safe_point`), `blockmanager.cpp` (`bm_DiscardBlock` → `tier2_on_discard`), `rec_arm64.cpp` (init/reset, fault na área recusado), `Makefile.common` | **ALTO** | **medido (jogo, 2 rodadas)** | 2026-09-24. Porte das regras do `tier2_gen.py --emu`; região por `FC_TIER2_RT=ENTRADAS/BLOCOS`; 1 MB na cauda do cache de código; instalação num ponto seguro (a cada 256 `UpdateSystem`): a 1a instrução do bloco antigo (`subs w27`) vira `b` para a região, guarda de entrada falhando refaz o `subs` e volta; bloco da região descartado desfaz os ganchos. **DOA2:** `state_compare` IDÊNTICO (1309 frames do TA, 21,8 s de áudio); região de 2164 bytes, guardas 23/23/25 (iguais ao gerador Python). **Velocidade (2 rodadas, antigo / tempo de execução / `.S` offline):** jogo 87,2·87,3 / 97,9·94,4 / 90,8·96,4%; intervalo do frame emulado 20,18·20,24 / 18,08·18,65 / 19,29·18,35 ms; underruns 67·64 / 17·30 / 47·24; fps 31,8·32,0 / 32,4·32,5 / 31,9·31,7; frame time médio 31,4·31,3 / 30,8·30,8 / 31,3·31,5; p50/p95/p99 34,8/42,5/54,7·34,6/41,5/52,5 / 32,2/38,5/51,2·32,3/39,2/51,3 / 33,7/41,2/55,8·32,5/39,5/51,8. Próximo: perfil por amostragem e formação automática de região (etapa 2). |
| 4.59 | **Nível 2 automático (etapa 2): perfil no fim da fatia + formação de região — DOA2 IDÊNTICO e 87 → 93%; Shenmue II IDÊNTICO, sem ganho mensurável (spill em volta da SQ)** | `core/rec-ARM64/tier2.cpp` (`FC_TIER2_AUTO=1`), `rec_arm64.cpp` (`intc_sched` grava x29 em `t2_last_pc`) | **ALTO** | **medido (jogo)** | 2026-09-24. **Perfil:** amostra = bloco que rodava quando a fatia de ciclos acabou (o `intc_sched` já tem o retorno em x29), 1 a cada 64 fatias (~7 mil/s), cache do último bloco. Tentativas que custaram e foram trocadas: timer de 1 kHz por sinal (-6% no DOA2) e toda fatia amostrada (busca no mapa de blocos a ~450 mil/s, -12%); com a versão final o intervalo do frame emulado só com perfil é igual ao do JIT antigo (20,75 × 20,73 ms). **Formação:** blocos quentes (80% das amostras) aceitos pelo gerador, unidos pelas arestas estáticas; todos os blocos viram entrada (saída para bloco da própria região vai para a retomada, não para o despachante); só região com laço; recusa definitiva marca os blocos; guarda de volta primeiro no início dos blocos que chamam a SQ, depois os ciclos restantes (DFS a partir de quem entra de fora). Gerador: fim de bloco dinâmico (`rts`/`jsr`/`jmp`) como saída, grupos de store atravessam a chamada da SQ (x6/x7 salvos). **DOA2:** IDÊNTICO (1264 frames, 21,1 s de áudio); 2 rodadas: velocidade 87,0·86,7 → 93,1·93,3%, intervalo do frame emulado 20,29·20,28 → 18,89·19,19 ms, underruns 67·65 → 37·44, fps 32,3·32,6 → 31,2·31,2, frame time médio 30,9·30,6 → 32,0·32,0, p50/p95/p99 34,3/41,5/52,2·34,0/41,0/53,5 → 33,3/40,9/60,1·33,2/41,7/52,8. **Shenmue II:** laço de vértices (15 blocos) entra; IDÊNTICO (449 frames, 15 s de áudio); velocidade 58,2·60,3 → 60,8·60,3 (ruído). Causa medida: 88 spills em 8 chamadas da SQ (~11 floats por chamada; a chamada fica no meio do bloco com o próximo vértice em registrador); no DOA2 ~1. Próximo: adiar a descarga da SQ até a próxima guarda (cópia de 32 bytes por `pref`, uma chamada ao TA na guarda), prioridade de callee-saved para valores vivos em chamada; depois a segunda thread (etapa 3) e cobertura (etapa 4: mais de 8 GPR escritos, stores em RAM, fault seguro). |
| 4.60 | **Nível 2, etapa 3: compilação na segunda thread + autochecagem das regiões; bugs achados nos outros 3D (fault em MMIO, laço linear travando a análise, SQ marcada como acesso lento)** | `core/rec-ARM64/tier2.cpp` (`submit`/`finish`/`worker_loop`, `check_regions`, contadores `cntEntries`/`cntPasses`, `tier2_fault`, `slowMem`), `core/libretro/common.cpp` (fault na região emulado), `rec_arm64.cpp` (`RewriteCompactMem` marca bloco com acesso lento, menos SQ) | **ALTO** | **implementado; avaliado jogando** | 2026-09-24. Análise na emu thread, emissão na thread de compilação (0,15-2 ms por região), instalação no ponto seguro se os blocos ainda são os mesmos; reset troca a geração. Cada região conta entradas e voltas; com < 1,5 volta por entrada (caminho quente saindo da região quase toda volta) os ganchos são desfeitos. **Bugs:** (1) Zombie/Giga Wing 2: região de 1 bloco num laço de espera lendo 0xA05F6C18 (Maple/Holly) → fault dentro da região parava o core → bloco com acesso reescrito para trampolim (MMIO/OCRAM/página de código) fica fora, e leitura fora da RAM na região é emulada no handler; (2) essa marcação pegava também os stores na SQ → laço de vértices do DOA2/Shenmue II recusado "sem laço" → SQ não marca; (3) Zombie congelava: a subida da guarda de volta girava para sempre num laço linear (A→B→C→A) dentro do `UpdateSystem` → para ao dar a volta; (4) limite da autochecagem em 3 voltas removia o laço do DOA2 (strips curtas, ~2 voltas) → 1,5. **Uma rodada por jogo (FC_TIER2_AUTO=1):** Zombie 99,0% (antigo ~98), região ruim removida (0,14 voltas), 3 boas; DOA2 região do laço mantida, frame emulado 19,0 ms (antigo ~20,3), underruns 40 (antigo ~67), VEL 86,4% num regime de fila de render com fps 29; Shenmue 1 84,8%, região ruim removida (0,97); Giga Wing 2 84,9%. **Jogando (usuário):** DOA2 jogável; Giga Wing 2 jogável a ~45 fps parecendo 100%; Shenmue 1 26 fps, mais rápido que ontem; Zombie congelou na versão com o bug (3), corrigido. |
| 4.61 | **"Técnica" do Shenmue II: motor grande em C com funções pequenas (27,8% dos fins de bloco em chamada/retorno, 15 mil blocos, ~300 pontos alimentando o TA) — o despacho de chamada/retorno custa pouco (hipótese refutada); o custo está no cache de instruções e no CHD** | dumps `FC_JIT_DUMP` (Shenmue II, Shenmue 1, DOA2), PMU da emu thread | **ALTO** (diagnóstico) | **medido** | 2026-09-24. **Estilo de código (dump):** Shenmue II 15.040 blocos, chamada/retorno/indireto em 27,8% dos fins de bloco (DOA2 13,6%, Shenmue 1 18,8%), 298 sites de SQ (DOA2 204), 8,5 instr SH4 por bloco. Shenmue 1 é outro padrão: OCRAM intensa (527 sites) e 3.956 escritas em página de código (overlays). **Hipótese testada: despacho de `jsr`/`rts` (saída dinâmica, FPCB, salto indireto mal previsto) seria o custo do estilo — refutada.** PMU da emu thread, 12 s, JIT antigo: 15,53 G ciclos, 6,73 G instruções (IPC 0,43); 858 M desvios previstos, 89 M errados (10,4%); indiretos errados 14,1 M (1,18 M/s, 80% no código JIT) ≈ 0,7% dos ciclos a ~8 ciclos cada; condicionais errados 74,6 M ≈ 3,8%. Com o estudo anterior (despacho total ~2,5-3,5%, `docs/jit_study.md`), otimizar chamada/retorno à la Dolphin (`bl`/`ret`) daria no máximo ~2-3%. O estilo custa no cache de instruções (21% dos ciclos em parada de L1I; código quente ~266 KB contra L1I 32 KB / L2 256 KB) e na espera de dado (17,6%); a descompressão do CHD é ~15% da emu thread. Ordem sugerida: CHD fora da emu thread; código menor no nível 1 para o motor inteiro; chamada/retorno por último. |
| 2.1 | Varredura incondicional de 64 canais AICA | `core/hw/aica/sgc_if.cpp:1356-1374` | MÉDIO→BAIXO | descartado (por composição) | Coberto pelo agregado medido em 2.3 (~1ms total AICA+ARM7/s) — não vale investigar detalhe interno sem indício de custo maior |
| 2.2 | Fallback de interpretador ARM7 por instrução (LDM/STM) | `core/hw/arm7/arm7.cpp:1933-1967` | MÉDIO→BAIXO | descartado (por composição) | Idem — coberto pelo agregado de 2.3 |
| 2.3 | `arm_mainloop` não separável de `libAICA_TimeStep` | `core/hw/arm7/arm7.cpp:1535-1543` | MÉDIO→**descartado** | descartado | Instrumentado e medido em 2026-09-13: `arm_mainloop=0-1us`, `libAICA_TimeStep=0us` por chamada (~44.100 chamadas/s ⇒ ~1ms total de ARM7+AICA por segundo real, distribuído entre ~30 frames). Seção 2 inteira (AICA/ARM7) descartada como causa do gargalo de ~33ms/frame da `emu_thread`. |
| 2.4 | I/O de CD-DA dentro do loop de mixagem de áudio | `core/hw/aica/sgc_if.cpp:1385-1470` | BAIXO | não investigado | Só relevante com faixas CD-DA tocando |
| 2.5 | DSP da AICA (efeitos) | `core/hw/aica/dsp_arm64.cpp:244-460` | BAIXO | não investigado | Off por padrão |
| 2.6 | `FlushCache()` ARM7 — O(2M) por reset | `core/hw/arm7/arm7.cpp:2014-2019` | BAIXO | não investigado | Raro/pontual |
| 2.7 | Fronteira `WriteSample`/`audio_batch_cb` | `core/libretro/audiostream.cpp:10-23` | BAIXO | não investigado | |
| 3.1 | `ta_parse_vdrc()` — parsing completo da display list | `core/hw/pvr/ta_vtx.cpp:1545-1639` | ALTO | **descartado (medido)** | **Medido em 2026-09-16** com `FC_TA_SPLIT` (novo) no savestate pesado do mslug6: o `Process()` inteiro é ~57ms de um frame de ~63ms, mas o **parsing em si custa ~1ms**. Dos 42,1ms do walk de decode (`TaCmd`), **41,2ms (98%) são `GetTexture`** — cache de textura, não parsing. A cena tem só 560 polígonos e 88KB de display list. **A suspeita original (parsing caro) está descartada**; o custo real virou o item 4.12, já corrigido. |
| 3.2 | `make_index()` — custo por vértice, lista `tr` domina | `core/hw/pvr/ta_vtx.cpp:1417-1495` | ALTO | **descartado (medido)** | **Medido em 2026-09-16** com `FC_TA_SPLIT` (novo), mslug6, savestate pesado: `make_index` (as três listas op/pt/tr juntas) custa **0,152 ms/frame** — desprezível frente aos ~63ms do frame. A hipótese de que a lista `tr` dominaria por custo por vértice **está refutada**: o que domina é upload de textura (item 4.12). `fix_texture_bleeding` no mesmo corte deu 0,000ms. |
| 3.3 | `fix_texture_bleeding()` recomputado do zero toda cena | `core/hw/pvr/ta_vtx.cpp:1497-1541` | MÉDIO | **descartado (medido)** | **Medido em 2026-09-16** com `FC_TA_SPLIT` (novo), mslug6 savestate pesado: **0,000 ms/frame**. Só roda quando `screen_height > 480`, o que não é o caso neste device. Descartado. |
| 3.4 | `CaclulateSpritePlane()` — 2 divisões float por sprite | `core/hw/pvr/ta_vtx.cpp:1220-1267` | MÉDIO | não investigado | Escala com nº de partículas |
| 3.5 | `GetTexture()` disparado por polígono/sprite | `core/hw/pvr/ta_vtx.cpp:712,780,1174` | MÉDIO | **confirmado — ERA o gargalo, corrigido via item 4.12** | **A auditoria estática original acertou este.** Medido em 2026-09-16 com `FC_TA_SPLIT` (novo) no savestate pesado do mslug6: **558 chamadas de `GetTexture` por frame custando 41,2ms** (74,8µs cada) — **98% do walk de decode da TA e ~65% do frame inteiro**. Não era o parsing (item 3.1, ~1ms) nem `make_index` (item 3.2, 0,15ms): era textura. A causa raiz não era a frequência das chamadas em si, mas o que cada uma fazia: 70% resultavam em reconversão + re-upload, e **todas** pegavam o caminho caro de CPU porque o caminho de paleta na GPU estava desativado por um default não inicializado (**item 4.12**, corrigido). Depois do fix: tempo de textura 42,5 → 20,6 ms/frame, fps do mslug6 +35%. Fica registrado que este item passou ~3 dias como `não investigado` enquanto a investigação perseguia CPU/JIT — o `perf` apontava `SH4_TCB` com 53,9%, o que levou a interpretar consumo de CPU como caminho crítico. Ver `docs/texcache_vram_invalidation_plan.md`. |
| 3.6 | Pool exhaustion de `TA_context` → aloc. ~15MB sob backlog | `core/hw/pvr/ta_ctx.cpp:209-244` | ALTO→**descartado** | descartado | Instrumentado e medido em 2026-09-13/14: só 2 alocações `new` nas primeiras 60 chamadas (aquecimento), depois **zero** novas alocações em toda a janela de 90s medida (100% servido pelo pool de 2). Pool nunca esgota nesta cena — sem loop de retroalimentação negativa aqui. |
| 3.7 | Sincronização emu↔render (`rs.Wait`/`re.Wait`) | `core/hw/pvr/Renderer_if.cpp:181-233,304` | BAIXO | confirmado | Medido em 2026-09-13: `rsWait` domina o "core time" externo em trechos calmos (~24-28ms de ~26-30ms), mas é só espera pela `emu_thread`, não trabalho — ver achado arquitetural no topo deste arquivo |
| 3.8 | Buscas lineares em `ctx_list`/`spg_line_sched` | `core/hw/pvr/ta_ctx.cpp:246-283`, `core/hw/pvr/spg.cpp:64-170` | BAIXO | não investigado | Custo fixo, não escala com geometria |
| 4.1 | `GenSorted()` — sort por triângulo + alocação oculta no `stable_sort` | `core/rend/sorter.cpp:205-366` | ALTO→N/A | **não aplicável neste device** | Instrumentado em 2026-09-13, mas **nunca disparou** — `config::PerStripSorting`/`AlphaSortMode` está configurado pra per-strip (`SortPParams`, item 4.2), não per-triângulo (`GenSorted`/`DrawSorted`). Item permanece válido apenas se alguém mudar a config de sorting; sem ação necessária agora. |
| 4.2 | Overhead de `glDrawElements` × contagem de draw calls (não `SetGPState` em si) | `core/rend/gles/gldraw.cpp:281-297` (`DrawList`), path real é `SortPParams`+`DrawList<Translucent,true>` (per-strip), **não** `GenSorted`/`DrawSorted` (per-triângulo, nunca ativo neste device) | ALTO | **corrigido (batching implementado e validado visualmente)** | Medição de 2026-09-13 no pico (`render=25587us`): `SortPParams`≈0,33ms (622 PolyParams/chamada, irrelevante), `SetGPState`≈2us/chamada×622≈1,2ms (irrelevante), **`glDrawElements`≈24us/chamada×622≈15ms — domina, ~60% do `render` total**. Causa raiz é overhead de submissão por draw call no driver Mali r13p0, multiplicado por ~500-600 strips/frame, não lógica de CPU. **Candidato nº1 de otimização real: reduzir contagem de draw calls (batching), não mexer em `SetGPState`.** **⚠️ RISCO CONHECIDO (relatado pelo usuário 2026-09-14):** o Gemini já mexeu em código próximo a `glDrawElements`/submissão de draw calls numa tentativa anterior — ganhou velocidade, mas **quebrou a renderização dos personagens no Shenmue** (regressão visual, não só de performance). Qualquer tentativa de batching aqui precisa ser validada visualmente (personagens renderizando corretamente), não só por benchmark de FPS — fácil de otimizar a métrica errada e entregar um jogo mais rápido e visualmente quebrado. (Nota: há também um caso relacionado já revertido em `docs/history.md` 2026-09-14 sobre cache de estado em `gles.h`/`gles.cpp`/`glcache.h` que quebrou personagens e foi revertido — pode ou não ser o mesmo episódio.) **2026-09-14, CORRIGIDO:** implementado batching via GLES3 primitive restart em `DrawList` (agrupa strips consecutivas com mesmo estado de GPU num só `glDrawElements`, gate `gl.is_gles && gl.gl_major>=3`). **Validado visualmente pelo usuário assistindo o Shenmue rodar ao vivo no device: ganho real de performance, sem quebra de renderização** (o risco acima não se repetiu). Benchmark oficial (90s warmup+90s, boot real) vs. baseline original: `core_average` 26,10→24,27ms (-7,0%), `video_average` 11,11→10,445ms (-6,0%), fps 26,87→28,79 (+7,1%). Contribuição isolada do batching (separada do regalloc, item 4.9) não está 100% isolada por não-determinismo do boot real (sem savestate do Shenmue) — ver `docs/history.md` pra tabela e ressalvas completas. |
| 4.3 | `std::map` (árvore) em `TexParameteri` | `core/rend/gles/glcache.h:186-215` | MÉDIO | não investigado | Amplificado pelo item 4.2. Uma tentativa de converter pra state cache quebrou a renderização de personagens e foi revertida. |
| 4.4 | Refresh de uniforms de todos os shaders da sessão, todo frame | `core/rend/gles/gles.cpp:868-879` | MÉDIO→**descartado** | descartado | Instrumentado e medido em 2026-09-14: custo cresce de 11us (2 shaders) até 50-60us (9 shaders) ao longo da sessão — mesmo no pico, irrisório frente aos 25ms de `render` em cena pesada. |
| 4.5 | `vidx_sort` não-`static` — malloc/free por frame | `core/rend/gles/gldraw.cpp` (`SortTriangles`) | MÉDIO | não investigado | Teste A/B simples: tornar `static` |
| 4.6 | `TexCache::CollectCleanup` varre o cache inteiro todo frame | `core/rend/TexCache.h:787-807` | MÉDIO→**descartado** | descartado | Instrumentado e medido em 2026-09-14: custo cresce de 1us (cache com 5 texturas) até 20us (cache com 41) ao longo da sessão — mesmo no pico, irrisório frente aos 33ms/frame. |
| 4.7 | Conversão de textura por-texel / `PrintTextureName` | `core/rend/TexCache.cpp:527-730,381-402` | BAIXO | não investigado | Conversão é esperada; log é desperdício menor |
| 4.8 | Upload de VBO/IBO completo todo frame (orphaning) | `core/rend/gles/gles.cpp:941-956` | BAIXO | não investigado | Design esperado, custo cresce com vértices |
| 4.9 | JIT arm64 regalloc só usa 8 int e 8 float físicos (spill pesado) | `core/rec-ARM64/arm64_regalloc.h`, `rec_arm64.cpp` | ALTO→**regressão em 2D confirmada por A/B real** | confirmado | Descoberto em 2026-09-14: O JIT só mapeava 8 registradores FPU (`S8-S15`). Alterado para mapear os "caller-saved" (`S16-S31`) com stack explícita (`PushCPURegList`/`PopCPURegList` em `PushCallerSaved`/`PopCallerSaved`, chamado de dentro de `GenCallRuntime`) no runtime call, subindo de 8 para 24. A tentativa com registradores inteiros (`W9-W15`) causou crash por serem usados hardcoded como *scratch* pelo próprio emulador no `rec_arm64.cpp` — só os inteiros foram revertidos, os 16 floats extras (`S16-S31`) seguem ativos. **Benchmark em Shenmue (3D, `retrorun3 --benchmark`): +5,5% FPS (28,35 vs 26,87), core_average 25,47ms vs 26,10ms, video_average 9,78ms vs 11,11ms — ganho real e medido.** **2026-09-14 (sessão seguinte), relato do usuário jogando MBAA (2D, interativo, SEM benchmark formal ainda): jogos 2D ficaram *mais lentos* com essa mudança; 3D só "um pouco melhor".** **2026-09-14, confirmado por A/B real** (mesmo savestate/duração, trocando só o `.so`, `flycast2021_libretro.so` puro no device — que sobrou intocado — vs. o `flycast_libretro.so` do Gemini, em `kofnw`/KOF Neowave como proxy de 2D já que MBAA não tinha savestate pronto): 1ª medição (binário do Gemini ainda com a instrumentação `chrono` desta sessão presente, ver achado abaixo) deu `core_average` 11,730ms (puro) → 12,674ms (Gemini), +8,0%; `core_frames` 1512 → 1437, -5,0%. **Corrigido em seguida** (removida a instrumentação `chrono`/`perfinstr_*` dos 8 arquivos que a continham — só ruído de medição, nenhuma delas é o regalloc — e refeito o rebuild+deploy+teste do zero, `make clean` incluído por causa de um `.o` de `TexCache.h` que ficou desatualizado num rebuild incremental): **número real, limpo, sem confound: `core_average` 11,730ms → 12,106ms (+3,2%); `core_frames` 1512 → 1475 (-2,4%); `video_average` 8,108 → 8,217ms (~ruído)**. Ou seja, quase metade da regressão aparente era a própria instrumentação de profiling que a sessão vinha deixando no código — a regressão real do regalloc em 2D existe mas é menor do que parecia (~3% de `core_average`, não ~8%). Ver `docs/history.md` pras duas tabelas completas (antes/depois da limpeza). **Hipótese arquitetural confirmada como causa provável (ainda não isolada por instrumentação direta de `GenCallRuntime`, mas consistente com o padrão medido):** `PushCallerSaved`/`PopCallerSaved` rodam em TODO `GenCallRuntime`, inclusive `UpdateSystem` (`rec_arm64.cpp:1431`, já medido como ~7.400 chamadas/frame no item 1.2) e os slow-paths de `ReadMem*`/`WriteMem*` (linhas 1100-1166) — ou seja, o custo de push/pop de pares de D-regs é pago em TODO bloco SH4 executado, não só nos que se beneficiam de mais registradores. Jogos 2D como MBAA tendem a ter blocos SH4 menores/mais numerosos (mais branches de lógica de jogo, menos aritmética de transformação 3D por bloco) e mais tráfego de I/O mapeado em memória (paleta, VRAM, registradores do PVR por sprite) — ou seja, pagam a taxa de push/pop com a mesma frequência (ou mais) que o 3D, mas sem o benefício de menos spill (blocos curtos raramente precisavam de mais de 8 regs mesmo antes). Precisa de instrumentação (contagem de `GenCallRuntime`/frame e nº médio de regs empurrados) pra confirmar antes de agir. |
| 4.10 | Confirmado por `perf`+gdb ao vivo: a função mais quente pós-fix da SQ é o trampolim compartilhado `intc_sched` (`UpdateSystem`+`rdv_DoInterrupts`), não memória nem renderização | `core/rec-ARM64/rec_arm64.cpp:1417-1449` (label `intc_sched`, dentro de `generate_mainloop()`) | ALTO (confirma hipótese do item 4.9) | confirmado | 2026-09-15, cena mais pesada de Metal Slug 6 (savestate do usuário, "monstro gigante mecânico de muitas partículas diferenciadas") — pedido do usuário pra usar MS6 como medidor da "função mais cara" agora que o item 1.7 (SQ) tá resolvido. `perf record -g --call-graph dwarf -F 999` (2 capturas independentes, ~24-37k amostras cada) mostrou uma única instrução dominando (~10-20% de TODAS as amostras, contra o resto disperso em centenas de blocos diferentes). **Achado inicial enganoso:** o offset relatado por `perf script`/`perf report` pro endereço da amostra veio consistentemente com os 2 bits baixos "sujos" (`endereço % 4 == 3` em TODAS as amostras — um PC real de ARM64 nunca pode ser assim, já que instruções são sempre alinhadas em 4 bytes) — subtrair 3 do endereço reportado sempre recupera o alinhamento correto; sem corrigir isso, uma tentativa de desmontar o endereço "cru" gera lixo (`.inst ... undefined`) que pode levar a conclusões erradas (ex.: um `bl` decodificado a partir de bytes desalinhados apontou pra dentro de uma região gigante `PROT_NONE` da reserva de 4GB do nvmem, o que pareceria um bug grave mas era só artefato de desalinhamento). Corrigindo o offset e confirmando via gdb ao vivo (`x/Ni` no endereço exato, com o `bl` resolvendo pro símbolo real `<UpdateSystem>`/`<rdv_DoInterrupts>` dentro do próprio `SH4_TCB`): a instrução quente é `bl SH4_TCB+3072` — uma chamada, embutida no final de blocos SH4 compilados (quando o contador de ciclos local do bloco estoura, `w27`), pro trampolim compartilhado `intc_sched` que já existia (gerado uma vez em `generate_mainloop()`, não é código novo desta sessão) e que chama `UpdateSystem()`/condicionalmente `rdv_DoInterrupts()`. **Confirma a hipótese arquitetural do item 4.9** (que já apontava `UpdateSystem` como chamado ~7.400x/frame e pagando `PushCallerSaved`/`PopCallerSaved` — hoje varrendo até 24 registradores FPU — em TODA chamada) com medição real de profiling, não só análise estática. `SH4_TIMESLICE=448` (ciclos SH4 entre checagens) é compartilhado entre TODOS os backends (x86/ARM32/ARM64, `core/hw/sh4/sh4_interpreter.h:57`) — não é algo desta sessão nem específico do ARM64, e mexer nesse valor trocaria overhead por precisão de timing de interrupção pra TODOS os jogos, não só o item mais caro desta cena; não é candidato de baixo risco. **Não corrigido ainda** — próximo candidato natural é o item 2 do `docs/arm64jit_improvement_plan.md` (reduzir o custo de `PushCallerSaved`/`PopCallerSaved` por chamada, já classificado como baixo risco/esforço), não decidido ainda com o usuário. **Conclusão importante:** a cauda pesada de Metal Slug 6 (item 1.7) NÃO é dominada por escrita de memória (resolvido) nem por draw calls/renderização (hipótese anterior, não confirmada por este profiling) — é overhead de despacho/agendamento de bloco (checagem de ciclo+interrupção), amplificado pela cena ter muitos objetos/blocos SH4 curtos executando em sequência (bate com a descrição do usuário: "monstro gigante mecânico feito de muitas partículas diferenciadas"). **CORREÇÃO (mesmo dia, antes de agir):** a frase acima "confirma a hipótese do item 4.9" estava ERRADA — verificado via desmontagem completa do próprio `intc_sched` que NÃO HÁ NENHUM `stp`/`ldp` de registrador ao redor do `bl UpdateSystem`/`bl rdv_DoInterrupts` (push/pop vazio). Causa: `generate_mainloop()` cria seu próprio `Arm64Assembler`/`regalloc` do zero (`compiler = new Arm64Assembler()`, mesmo padrão usado por `ngen_Compile` pra CADA bloco — confirmado lendo `rec_arm64.cpp:2194,2303`) e nunca chama `Preload_FPU`/`DoAlloc` antes de gerar `intc_sched` — `reg_alloced` fica vazio nesse ponto, então `PushCallerSaved`/`PopCallerSaved` calculam uma lista vazia e não emitem nada. **O item 4.9 não se aplica a este call site específico**; o Item 2 do plano de melhoria (reduzir custo de push/pop) não atacaria este achado. **Identificação do bloco quente por endereço-fonte real** (não só heurística de endereço): usando `bm_WriteBlockMap()` (já existente e ativa no binário, chamada ao vivo via gdb construindo manualmente um `std::string` fake — `bm_WriteBlockMap` está oculta do dynsym mas presente no symtab completo, `nm` sem `-D` encontra; `print_blocks()`, a alternativa mais simples, está morta dentro de um `#if 0` que cobre toda a função — tentativa de reativar com `__attribute__((used))` não teve efeito porque o pré-processador nunca chega a ver o código, e foi revertida) — o bloco contendo o endereço quente é **vaddr SH4 `0x8C05B3A4`, 6 opcodes, `guest_cycles=7`, `host_code_size=124 bytes`**: `mov32 r3,0x8c386928 / readm r1,[0x8c386928] / mov32 r0,19 / readm r2,[r15+19] / setae sr.T,r1,r2` — ou seja, uma leitura de 2 palavras (um endereço fixo de RAM de trabalho + um offset da pilha) seguida de uma comparação, provavelmente uma checagem de contador/limite (ex. "índice de partícula < limite") repetida do lado do PRÓPRIO JOGO. Blocos vizinhos no mesmo "caminho quente" (`8C05B3D0`, `8C05B3D6`, `8C05B68E`) são igualmente minúsculos (2-3 opcodes cada). **Isso não é ineficiência do emulador traduzindo esse código específico — é a lógica do próprio jogo (bookkeeping de objeto/partícula) executando um número enorme de vezes nessa cena**, e o `bl intc_sched` fica visível no profile só porque QUALQUER bloco, ao ser executado tantas vezes, acaba sendo "a vez" de estourar o orçamento compartilhado de 448 ciclos com frequência proporcional ao seu próprio nº de execuções. **Não há alavanca de baixo risco identificada do lado do JIT pra esse achado específico** — nem Item 2 (push/pop já vazio aqui) nem encolher o SH4 traduzido (já é o mínimo: 6 opcodes). A única alavanca conhecida é frequência de checagem (`SH4_TIMESLICE`), explicitamente adiada pelo usuário pra depois. **Testada em seguida (mesmo dia) como speedhack opt-in — resultado negativo, ver abaixo.** |
| 4.11 | `SH4_TIMESLICE` como speedhack opt-in (`reicast_sh4_timeslice`, 1x/2x/4x/8x) — testado no achado 4.10, **sem ganho medido** | `core/hw/sh4/sh4_interpreter.h`, `core/hw/sh4/interpr/sh4_interpreter.cpp`, `core/rec-ARM64/rec_arm64.cpp` (`generate_mainloop()`), `core/libretro/libretro.cpp`/`libretro_core_options.h` | BAIXO (não entregou o ganho esperado) | confirmado, **descartado como solução pro achado 4.10** (infra mantida, segura) | Implementado como opção de core libretro (categoria "hacks", label "(Restart Required)", default `1x` = comportamento idêntico ao anterior) em vez de mudar a constante direto, exatamente pra poder testar sem compromisso: `SH4_TIMESLICE` (macro, 448, inalterada) virou a base de uma nova variável `sh4_sched_timeslice` (default = `SH4_TIMESLICE`), lida uma vez em `update_variables()` e usada em todo lugar que decide a FREQUÊNCIA da checagem de interrupção/timer — os 4 sites em `generate_mainloop()` (JIT ARM64, valor lido em tempo de geração de código e cravado como imediato, igual o padrão já usado pelo resto do mainloop) e os 2 sites equivalentes no interpretador (`Sh4_int_Run`, `UpdateSystem()`) pra manter os dois caminhos consistentes. **Validado que a opção realmente funciona:** lida a variável ao vivo via gdb com `4x` configurado no `.cfg` → `1792` (`=448×4`), confirmando que o valor chega corretamente até o código gerado. Sem crash, `1x` (default) mede igual ao binário anterior (~21,8-22,0ms, dentro do ruído normal já visto). **Medição do ganho, porém, inconclusiva/negativa:** 1ª rodada em `4x` deu 22,3ms (nenhuma melhora sobre ~21,8-22,0ms do `1x`); 2ª rodada em `4x` deu 39,2ms, mas essa rodou colada em 3 benchmarks anteriores sem pausa — o mesmo artefato de contaminação térmica/recursos já documentado no achado 4.10 (recupera com ~90s de idle) — **não é um número confiável**, mas também não é evidência de ganho. Antes de conseguir uma repetição limpa (com pausa adequada), o usuário decidiu parar e confirmou: **"não teve ganho"**. **Conclusão:** a opção fica no código (segura, opt-in, default inalterado, testada sem crash) como ferramenta disponível pra experimentação futura — mas não resolve o achado 4.10 nesta cena/jogo. Hipótese pra por que não ajudou (não testada): o overhead do `bl intc_sched` visto no profile pode não estar no caminho crítico do tempo de frame (a CPU pode estar terminando seu trabalho antes do vsync/GPU de qualquer forma nessa cena, então economizar ciclos de CPU não muda o tempo de frame percebido) — precisaria de instrumentação adicional (ex. medir tempo ocioso de CPU por frame) pra confirmar, não feito. |
| — | Descartado: hash de textura por frame | `core/rend/TexCache.cpp` (`ComputeHash`) | — | descartado | Só roda com `DumpTextures`/`CustomTextures` ativos |
| — | Descartado: `glCheck()`/`glGetError` no hot path | `core/rend/gles/gles.h:38` | — | descartado | Macro vazia em release |
| — | Descartado: post-processing | `core/rend/gles/postprocess.cpp` | — | descartado | Desabilitado neste fork |

| 5.1 | `glInvalidateFramebuffer(depth+stencil)` no fim de `RenderFrame()` (item 1 de `docs/rendering_improvement_plan.md`) | `core/rend/gles/gles.cpp` (`findGLVersion()` resolve via EGL, chamada no fim de `RenderFrame()`) | Baixo (arquiteturalmente correto per ARM, mas resultado misto na medição) | **medido, misto — mantido** | Implementado 2026-09-15/16: resolvido manualmente via `eglGetProcAddress` porque a tabela de símbolos deste fork (`glsym_private.h`, `HAVE_GLSYM_PRIVATE`) não cobre GLES3 core, só GLES2+extensões. Sem crash em kofnw/Metal Slug 6/mbaa. **Metal Slug 6, protocolo oficial (`--benchmark 60 --benchmark-warmup 15`, mesmo savestate/cena, 2 rodadas), comparado com a baseline pós-fix-da-SQ já documentada no item 1.7 (`core_average`=21,00ms, `core_frames`/60s=1932): `core_average` 21,00→21,397ms (+1,9%) e →21,660ms (+3,1%); `core_frames`/60s 1932→1916 (-0,8%) e →1910 (-1,1%). As duas rodadas concordam na direção — regressão pequena mas real, não ruído.** Cena é CPU-bound (já flagada pelo usuário como "nunca vai ficar mais leve" nesse jogo) — não há banda de framebuffer sobrando pra `glInvalidateFramebuffer` aliviar aqui, então só sobra o custo fixo da chamada extra ao driver todo frame, sem benefício pra compensar. **mbaa (2D, GPU-bound em cenas de ação, testado por gameplay real do usuário, não benchmark automatizado):** usuário relatou pico novo de 50fps (teto histórico do jogo nesse device era 45fps) e sensação subjetiva de melhora; percentis de `active_frame` (frame time real, core+vídeo) das 2 rodadas com o fix: p50≈20,85-20,97ms (≈47,7-48,0fps), consistente com o teto antigo sendo superado no caso típico. **Sem baseline controlada equivalente pro mbaa** (o usuário optou por não jogar a mesma sessão no binário antigo — decisão dele, registrada 2026-09-16) — então esse resultado positivo é indício forte, não prova formal. `kofnw` (benchmark automatizado): sem diferença clara em nenhuma direção. **Mecanismo plausível pra reconciliar os 3 jogos** (não confirmado por instrumentação direta, mas consistente com achado de código já documentado): audit `docs/gles_code_audit.md` §5.3 já identificou que `QueueRender()`/`rqueue` é uma fila de slot único que descarta (frameskip) o próximo frame simulado se a thread de render ainda estiver dentro de `Render()` do frame anterior — ou seja, o tempo de `Render()` (que inclui a chamada nova de invalidate) não é só custo de CPU, é também a *janela* de frameskip. Em cena GPU-bound (mbaa em ação), se o invalidate reduz trabalho real de GPU (bandwidth de depth/stencil), `Render()` termina mais rápido do ponto de vista da thread de emu, o slot da `rqueue` libera mais rápido, e menos frames são descartados — dando "mais espaço pra respirar" (frase do usuário) sem precisar reduzir `core_average`. Em cena CPU-bound (Metal Slug 6), `Render()` nunca era o fator limitante de frame-rate, então esse mecanismo não tem como ajudar e só sobra o custo da chamada. **Decisão:** manter a mudança — é uso correto de API por documentação oficial da ARM (não é gambiarra), o custo medido é pequeno e concentrado exatamente na cena onde ele não pode ajudar mesmo, e o benefício potencial em cena GPU-bound (onde `rqueue`/item 2 do plano de renderização já era candidato a maior impacto) é real mesmo que não isolado formalmente ainda. Instrumentar o contador de descarte de `QueueRender()` (já sugerido no item 2 do `rendering_improvement_plan.md`) seria o próximo passo pra confirmar o mecanismo, se algum dia for retomado — não feito nesta sessão. |

| 5.2 | Skip de lista Translucent do PowerVR sob spike de frame time (speedhack, `reicast_frame_budget_skip_translucent`) | `core/rend/gles/gles.cpp`/`gldraw.cpp` (revertido) | Alto (corrupção visual real) | **implementado, testado, revertido — 2 bugs reais confirmados** | Pedido do usuário 2026-09-16: técnica pra pular a lista Translucent (`ta_structs.h:754-758`) quando o frame anterior estourasse uma baseline móvel (EWMA), preferindo "glitch a lentidão". Implementado com 3 níveis de agressividade, testado sem crash em kofnw/Metal Slug 6 (savestates automatizados). **Ao testar em Shenmue (boot real, sem savestate, cena variada) o usuário identificou visualmente 2 bugs reais, ambos confirmados por análise de código:** **(1) Lógica de detecção invertida do pretendido:** o threshold é relativo à baseline recente (EWMA) de cada trecho, não a um valor absoluto — numa cena estável (30fps), a baseline converge baixa e qualquer jitter normal do sistema cruza 1,4x dela, disparando sem motivo real; numa cena pesada e uniformemente lenta (25fps), a baseline converge alta e raramente varia o suficiente pra cruzar 1,4x dela mesma, então quase nunca dispara — exatamente o oposto do que o usuário observou precisar ("cenas que historicamente mantêm 30fps continuam mantendo mas com flicker; cenas de 25fps não melhoram e não têm flicker"). **(2) Corrupção visual real, não só efeito ausente:** o usuário relatou "frames preenchidos por cor predominante do jogo, destruído por completo", possivelmente persistindo por mais de 1 frame — não é o "faltou partícula/transparência" esperado. Investigação de código encontrou evidência concreta de que a lista Translucent deste pipeline **não é um bloco isolado sem efeito colateral**: o caminho de sort por-triângulo (`DrawSorted`, inativo nesta config mas presente no código) faz `glBindBuffer` pra um buffer de índice temporário e depende de um rebind explícito no fim (comentário original do projeto: `// Re-bind the previous index buffer for subsequent render passes`) e tem lógica de gravar no depth buffer especificamente "pro próximo render pass" (`TranslucentPolygonDepthMask`, comentário citando o jogo Cosmic Smash) — ou seja, esse pipeline já tem histórico documentado de estado acoplado entre a lista Translucent e os passes/frames seguintes. Pular o bloco inteiro (decisão de implementação desta sessão) pula qualquer preparação de estado que ele devesse fazer pra quem vem depois, sem visibilidade completa de tudo que depende disso — explica corrupção que persiste além de 1 frame (o estado errado só se corrige quando um frame SEM skip roda de novo). **Decisão: revertido por completo** (`git revert f77fa6d1c`, commit `de9a26625`) em vez de tentar mais patches — a superfície de risco (estado entre passes/frames num pipeline com histórico de fragilidade documentado, item 4.2 desta mesma tabela) é maior do que dá pra mapear com segurança só lendo código estático, e o resultado observado (corrupção real) é categoricamente diferente do que o usuário pediu (efeito visual ausente, não corrompido). Se a ideia for revisitada, precisaria: (a) uma métrica de "está lento" baseada em orçamento absoluto, não relativo à baseline recente; (b) preservar/replicar qualquer setup de estado que a lista Translucent normalmente faria, não pular o bloco inteiro. Nenhuma das duas foi tentada. |

| 5.3 | Skip de lista Translucent v2 — reimplementado com correção granular, orçamento absoluto e histerese | `core/rend/gles/gldraw.cpp` (reduz contagem, não pula bloco), `core/libretro/libretro.cpp` (`retro_run()`, decisão) | Médio-Baixo (arquitetura corrigida, calibração ainda em ajuste) | **implementado, em teste ativo — ganho real reportado pelo usuário, refinamento de calibração em andamento** | Reimplementação completa do item 5.2 corrigindo os 2 bugs que levaram ao revert: **(1) Granularidade:** em vez de pular o bloco "Alpha blended" inteiro, reduz só a CONTAGEM passada pra `DrawList<ListType_Translucent,...>` (`gldraw.cpp`) — `SortPParams`/sort continua rodando na lista completa (ordem de profundidade correta pro que for desenhado), nenhum setup/teardown de estado é pulado. Resultado visual confirmado pelo usuário em Shenmue: efeito contido (cabelo/detalhe translúcido picando), não mais corrupção de frame inteiro. **(2) Sinal de detecção:** `RenderFrame()` isoladamente não capturava gargalo de CPU (só a submissão de GPU) — trocado por `g_lastFrameTimeMs`, tempo do `retro_run()` INTEIRO (`rsWait` da emu\_thread + Process + Render + video\_cb, `core/rend/transform_matrix.h`/`libretro.cpp`), que é o que de fato bate com `core_average`/percentis reportados pelo `retrorun3`. Decisão movida pra dentro do próprio `retro_run()` (não mais em `gles.cpp`) pra manter o lag de 1 frame exato. Referência absoluta usa `g_declaredFps` (real taxa de sync do display via `SPG_CONTROL`, **não** fps interno do jogo — um jogo de "30fps" no Dreamcast ainda escaneia a 60Hz). **Dois bugs REAIS de processo encontrados e corrigidos no caminho, ambos de `make clean` incompleto** (não passar as mesmas flags do build pro `make clean` limpa a lista de objetos do platform errado, deixando `.o` arm64 antigos parados — causou 2 regressões de ~9x mais lento nesta sessão, sem erro/warning; ver novo item no `CLAUDE.md`). **Calibração, 2 rodadas de ajuste ao vivo pelo usuário jogando Shenmue:** primeira calibração (multiplicadores 1,5x/2,0x/3,0x de vblank) piscava em cena estável de 30fps nativo — porque 2,0x×16,66ms=33,33ms é EXATAMENTE o valor de 30fps nativo (2 vblanks), sentando o limiar em cima do ritmo normal do jogo, não de um desvio real. Recalibrado pra 1,8x/2,6x/4,0x (afastado de múltiplos inteiros de vblank). Ainda piscava um pouco, inclusive no logo de boot da Sega (conteúdo trivial, praticamente sem geometria translúcida) — confirmando que a causa não era só calibração, mas ausência total de tolerância a ruído de frame único (comparação instantânea, sem suavização). **Corrigido com histerese:** exige 2 frames CONSECUTIVOS acima do orçamento antes de reduzir (reseta a contagem a qualquer frame dentro do orçamento) — blips isolados de jitter de agendamento (natural em `ThreadedRendering`, onde `retro_run()` inclui espera pela emu\_thread) não disparam mais, só degradação sustentada. **Resultado após histerese:** disparos em Shenmue caíram de 2698 para 410 numa mesma janela de 90s de boot real; usuário reportou ao vivo "piscou bem menos, ficou bastante mais estável inclusive nas cenas pesadas" — ganho real confirmado visualmente, ainda com flicker residual leve em cena leve. **v2.4 (mesmo dia):** trocada a heurística de "melhor recente" (v2.3) por uma medição de fps real, validada contra o código-fonte de verdade do `retrorun3` (`navy1978/retrorun` no GitHub, público) — `g_measuredFps` replica exatamente a fórmula que o próprio contador de fps do retrorun usa (`ceil(totalFrames/elapsedSeconds)` numa janela de ~1s), confirmando que não existe metadado de "fps nativo por jogo" em lugar nenhum do sistema (nem no core, nem no retrorun) — é sempre medição ao vivo. **Incidente de crash intermitente investigado e não confirmado:** 1 `DEBUGBREAK`/SIGSEGV em 2 rodadas de Shenmue com a v2.4; investigação extensa (handler de SIGSEGV real não logou sua linha característica antes do crash, então não foi esse caminho; `isFlycast2021()`/versão do core no `retrorun3` batem certinho, então não foi o bug conhecido de `retro_unload_game()` documentado no próprio repo do retrorun; usuário confirmou visualmente não ter visto crash; retry rodou 100% limpo) — não reproduzido, causa não cravada, tratado como não-relacionado ao código desta feature. **Veredito final do usuário, após múltiplas rodadas A/B (opção ligada vs desligada, mesmo binário v2.4, mesmo protocolo de boot real):** a feature introduz glitch visual sem gerar ganho de performance mensurável — **descontinuada, mantida no código como opt-in mas desligada por padrão** (não removida, disponível para experimentação futura se alguém quiser retomar, mas não recomendada). Estado final no device: binário v2.4 deployado, opção `disabled` no `retrorun.cfg` oficial e em todos os `.cfg` de teste. |

## Achado arquitetural — RESOLVIDO em 2026-09-13

**Qual thread satura o core a 100%: `emu_thread` (SH4/AICA/ARM7) ou a thread que
chama `retro_run()` (TA parsing + sort + submissão GL)?**

Resposta medida (instrumentação em `core/hw/pvr/Renderer_if.cpp`, ver
`docs/current_plan.md` item 1 e `docs/history.md`): **a `emu_thread` domina**. Em
trechos calmos, `emuThread≈33ms/frame` vs. `process+render<1ms/frame` — a
simulação de CPU sozinha já consome quase todo o orçamento de 30fps. Em trechos
pesados (partículas), `render` cresce 10-25x (confirma a cascata do item 4.2) mas
`emuThread` continua sendo a maior fatia em termos absolutos.

**Impacto na priorização:** os itens ALTO da seção 1 (§1.1 `CheckBlock` anti-SMC) e
os itens da seção 2 (AICA/ARM7) sobem de prioridade — são os candidatos mais
diretos pra explicar por que a `emu_thread` já está no limite mesmo em cenas
calmas. Os itens da seção 3-4 continuam relevantes como explicação do
**agravamento** em cenas pesadas, mas não são mais o suspeito nº1 do baseline.

## Conclusão final da investigação candidato-a-candidato (2026-09-14)

Depois de testar TODOS os 8 itens da fila (`docs/current_plan.md`) e mais um
achado novo (throughput real do SH4), o quadro completo é:

1. **CheckBlock anti-SMC, AICA/ARM7, TA_context pool, sh4_sched_ffts, TexCache
   cleanup, shader uniform loop, GenSorted** — todos descartados por medição
   direta. Nenhum contribui de forma relevante.
2. **Compilação de bloco JIT** — contribui ~1,2% do tempo total, real mas não
   dominante.
3. **`glDrawElements` × contagem de draw calls** (item 4.2) — confirmado e
   quantificado: ~15ms dos ~25ms de `render` em cena pesada. **Único vilão de
   código/arquitetura confirmado.**
4. **Throughput real do SH4 (`SH4_SPEED_RATIO`)** — em trechos calmos fica em
   ~1,0 (tempo real, correto); em trechos pesados **cai pra 0,69-0,74**. Isso
   não é um bug — é o jogo genuinamente precisando de mais instruções SH4 por
   segundo do que o Cortex-A53 consegue simular quando a cena fica complexa
   (mais partículas, mais IA, mais física).

**Conclusão prática:** a lenteza em cena pesada tem DUAS causas reais e
independentes, ambas confirmadas por medição — não uma "bala de prata" única:
- **Lado de renderização:** overhead de `glDrawElements` por causa de contagem
  alta de draw calls (~500-600/frame) → **fixável via batching**.
- **Lado de CPU:** o próprio SH4 (via JIT) não consegue simular a lógica do
  jogo em tempo real quando a cena fica pesada → só melhora com **qualidade do
  código gerado pelo JIT** (menos instruções ARM64 por opcode SH4 traduzido),
  não há "bug" pontual pra caçar aqui.
